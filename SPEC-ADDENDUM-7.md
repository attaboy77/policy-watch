# SPEC 보완서 7 — 개별 기업 소식·업종 특화·해외 기준 처리
## Policy Watch v1.7

> **적용 방법**
> `C:\policy-watch\SPEC-ADDENDUM-7.md` 로 저장 후 Claude Code에 입력:
> `SPEC-ADDENDUM-7.md 읽고 §1부터 진행해줘`

---

# 0. 이번 검수에서 나온 부적합 항목

| 항목 | 유형 | 처리 |
|---|---|---|
| 두나무 "美 상장 확정 아냐…회계기준 전환도 사실무근" | 개별 기업 소식 | §1 |
| 삼전닉스에 몰려 '재무제표' 발표가 두려운 기업들…흑자도산 공포 | 시황·전망 기사 | §1 |
| 한국공인회계사회, 2025년 공동주택 감리 지적사례 공개 | 업종 특화 감리 | §2 |
| IASB 공개초안 '위험경감회계' 검토의견 조회 기한 연장 | 해외 기준 | §3 **판단 필요** |
| 한국회계기준원, 제149회 KAI Forum: IASB 공개초안 개최 | 행사 안내 + 해외 기준 | §3, §4 |

## 공통 원인

기존 필터는 **주제**(회계·세무)와 **적용 대상**(업종)을 본다. 위 항목들은 그 둘을 통과하지만 다른 축에서 부적합하다.

> **규제 자체에 관한 것인가, 특정 기업·업종에 관한 것인가?**

`두나무 회계기준 전환`은 회계 주제이고 금융업 키워드도 없지만, **한 회사의 개별 사정**이다. 규제가 바뀐 게 아니다.

---

# 1. 개별 기업 소식 제외

## 1-1. 설계

특정 기업의 경영 활동·재무 상황에 관한 보도를 제외한다. ADDENDUM-5의 `is_corporate_pr()`이 홍보성 동사(가동·맞손)를 잡았다면, 이번은 **기업 이벤트**를 잡는다.

```yaml
# data/filters.yml

company_events:
  # 자본시장 이벤트
  - 상장
  - 상장폐지
  - 재상장
  - IPO
  - 공모
  - 청약
  - 유상증자
  - 무상증자
  - 자사주
  - 지분 매각
  - 인수
  - 합병
  - M&A
  - 분할
  - 지주회사 전환
  # 실적·전망
  - 실적 발표
  - 어닝
  - 컨센서스
  - 영업이익
  - 매출 증가
  - 흑자 전환
  - 적자 전환
  - 어닝 서프라이즈
  - 목표가
  # 재무 위기
  - 흑자도산
  - 부도
  - 워크아웃
  - 법정관리
  - 회생절차
  - 자본잠식
  - 상장 적격성
  # 시황·전망 어투
  - 공포
  - 우려 확산
  - 비상
  - 초비상
  - 위기감
  - 술렁
```

```python
def is_company_event(title: str) -> bool:
    """개별 기업 소식·시황 기사 판정."""
    t = _norm(title)
    if not any(_norm(k) in t for k in COMPANY_EVENTS):
        return False
    # 제도 변경 신호가 강하면 제외하지 않음
    strong = ["개정", "제정", "공포", "입법예고", "공개초안", "기준서",
              "모범규준", "가이드라인", "질의회신", "예규", "의무화", "시행령"]
    return not any(_norm(k) in t for k in strong)
```

## 1-2. 제조업 회계 예외 (사용자 요청 반영)

제조업 회계기준과 직결된 내용은 개별 기업 사례라도 **통과시킨다.** 실제 적용 사례는 실무 참고 가치가 크기 때문이다.

```yaml
manufacturing_accounting_context:
  - 재고자산
  - 재고 평가
  - 저가법
  - 유형자산
  - 손상차손
  - 감가상각
  - 제조원가
  - 원가 배분
  - 매출원가
  - 수익인식
  - 진행기준
  - 리스회계
  - 개발비
  - 연구개발비 자산화
  - 충당부채
  - 복구충당부채
  - 환경충당
  - 배출권
```

```python
# is_company_event()가 True여도, 위 문맥이 있으면 통과
if any(_norm(k) in t for k in MANUFACTURING_ACCOUNTING_CONTEXT):
    return False
```

## 1-3. 판정 예시

| 제목 | 판정 | 근거 |
|---|---|---|
| 두나무 "美 상장 확정 아냐…회계기준 전환도 사실무근" | **제외** | 상장, 강한 신호 없음 |
| 삼전닉스에 몰려 '재무제표' 발표가 두려운 기업들…흑자도산 공포 | **제외** | 흑자도산·공포 |
| A사, 재고자산 저가법 적용 오류로 감리 지적 | **통과** | §1-2 제조업 문맥 |
| 상장기업 재고자산 평가 관련 K-IFRS 질의회신 공개 | **통과** | 강한 신호 |

---

# 2. 업종 특화 감리·회계 제외

## 2-1. 문제
`공동주택 감리 지적사례` — 아파트 관리비 회계에 관한 것으로 제조업과 무관하다.

## 2-2. 조치

ADDENDUM-6의 `applicability.excluded_entities`에 항목을 추가한다.

```yaml
applicability:
  excluded_entities:
    # 기존 financial / public / nonprofit / smb_only 에 이어서
    industry_specific:
      # 주택·건설
      - 공동주택
      - 아파트 회계
      - 관리사무소
      - 입주자대표
      - 재건축조합
      - 재개발조합
      - 정비사업조합
      - 주택조합
      # 협동조합·상호금융 (금융 항목과 별개)
      - 농협
      - 수협
      - 산림조합
      - 생협
      # 기타 특수 회계
      - 학교회계
      - 유치원 회계
      - 병원회계
      - 의료기관 회계
      - 사학기관
      - 국가회계
      - 지방자치단체 회계
      - 정당 회계
      - 정치자금
```

**주의:** `조합`이라는 단어 단독으로는 넣지 않는다. `노동조합`, `조합원` 등 다른 맥락에서 오탐이 발생한다.

---

# 3. 해외 기준 처리 ★ 판단 필요

## 3-1. 해외 기준은 성격이 갈린다

사용자 지시는 "해외 기준은 크롤링하지 말라"였으나, 실무 영향이 서로 다르므로 구분이 필요하다.

| 기관 | 성격 | 우리 관련성 |
|---|---|---|
| **IASB / IFRS** | K-IFRS의 원천. 한국은 IFRS를 전면 채택 | **높음** — 여기서 개정되면 1~2년 내 K-IFRS 개정으로 이어짐 |
| **ISSB** | 지속가능성 국제기준. KSSB가 이를 기반으로 제정 | **중간** — 국내 도입 시 반영됨 |
| EFRAG / ESRS / CSRD | EU 역외기업 대상 | 낮음 — EU 상장·대규모 사업 없으면 무관 |
| FASB / US GAAP | 미국 기준 | 낮음 |
| SEC 기후공시 | 미국 상장사 대상 | 낮음 |

회계기준원이 IASB 공개초안에 대해 검토의견을 조회하는 것은, **한국 의견을 IASB에 제출하기 위한 국내 절차**다. 완전한 해외 사안은 아니다.

## 3-2. 두 가지 선택지

### 안 A — 별도 분류 (권장)

IASB·ISSB 항목은 **수집하되 기본 조회에서 제외**한다. 상설자료·논의자료와 같은 방식이다.

```
doc_type에 "해외기준" 추가
→ 기본 조회 및 "오늘의 정책동향"에서 제외
→ 문서종류 필터의 "해외기준만" 버튼으로 조회 가능
→ 카드에 회색 "해외기준" 뱃지 표시
```

EFRAG·FASB·SEC는 ADDENDUM-6 §1의 `foreign_jurisdiction`으로 **완전 제외**를 유지한다.

**근거:** IASB 개정은 결국 K-IFRS로 들어온다. 지금 대응할 필요는 없지만, 나중에 "언제부터 논의됐나"를 확인할 때 기록이 남아 있는 편이 낫다. 조기 경보 가치도 있다.

### 안 B — 전면 제외

IASB·ISSB도 `foreign_jurisdiction`에 추가해 완전히 제외한다.

**근거:** IASB 단계에서 회계팀이 실제로 할 일은 없다. 대응은 K-IFRS 개정 공표 이후에 시작된다. 화면을 단순하게 유지하는 편이 낫다.

## 3-3. 결정 요청

> **사용자가 A 또는 B를 선택할 것.** 지시가 없으면 **안 A**로 구현한다.
>
> 안 B를 선택해도 나중에 `filters.yml`에서 한 줄 수정으로 안 A로 전환할 수 있도록 구현할 것.

## 3-4. 국내 도입 논의는 항상 통과

어느 안을 택하든, 다음 문맥이 함께 있으면 정상 항목으로 처리한다.

```yaml
foreign_exception_context:
  - 국내 도입
  - 국내 적용
  - 국내 영향
  - K-IFRS 반영
  - KSSB
  - 한국 도입
  - 국내 기업 대응
  - 도입 로드맵
```

예: `IASB 리스 기준 개정, K-IFRS 반영 시기는` → 통과

---

# 4. 행사·포럼 안내 제외 보강

## 4-1. 문제
`한국회계기준원, 제149회 KAI Forum: IASB 공개초안 '위험경감회계' 개최` — 행사 개최 안내다. ADDENDUM-4의 `ADMIN_NOISE_KEYWORDS`에 `포럼 개최`가 있으나 영문 `Forum`이라 매칭되지 않았다.

## 4-2. 조치

```yaml
admin_noise:
  # 기존 항목에 이어서
  - Forum
  - Conference
  - Seminar
  - Symposium
  - Workshop
  - 컨퍼런스
  - 심포지엄
  - 워크숍
  - 웨비나
  - 개최 안내
  - 참가 신청
  - 사전 등록
  - 발표자 모집
```

**추가 규칙:** `제N회` 패턴(`제149회`, `제12차` 등)이 제목에 있고 강한 규제 신호가 없으면 행사 안내로 판정한다.

```python
SERIAL_EVENT_RE = re.compile(r"제\s*\d+\s*[회차]")

def is_event_announcement(title: str) -> bool:
    t = _norm(title)
    if any(_norm(k) in t for k in ADMIN_NOISE_KEYWORDS):
        return True
    if SERIAL_EVENT_RE.search(title):
        strong = ["개정", "제정", "공표", "의결", "확정", "공개초안 발표"]
        return not any(_norm(k) in t for k in strong)
    return False
```

**주의:** `회계기준위원회 제12차 회의 결과 — 제1116호 개정 의결`처럼 강한 신호가 있으면 통과시켜야 한다. 위 코드가 이를 처리한다.

---

# 5. 처리 순서 (개정)

```
[수집 단계]
1) 어댑터별 원본 수집
2) is_event_announcement()   행사·운영 공지 제외            ← §4 보강
3) is_applicable()           적용 대상 판정                 ← §2 보강
4) classify()                카테고리 분류

[L3 전용]
5) min_tier 검사
6) has_regulatory_signal()   규제성 판정
7) is_company_event()        개별 기업 소식 제외            ← §1 신규
8) is_corporate_pr()         타사 홍보성 제외
9) is_noise()                투자·주가 노이즈 제외
10) match_tax_subject()      세법 세목 화이트리스트
11) is_incident_noise()      사건사고 제외

[공통]
12) 해외기준 분류            doc_type = "해외기준"          ← §3
13) relevance_score()
14) dedupe_similar_news()
15) 카테고리별 상한 적용
```

---

# 6. 테스트 필수 케이스

`tests/test_company_event.py` 신설.

| 제목 | 기대 | 검증 |
|---|---|---|
| 두나무 "美 상장 확정 아냐…회계기준 전환도 사실무근" | 제외 | §1 상장 |
| 삼전닉스에 몰려 '재무제표' 발표가 두려운 기업들…흑자도산 공포 | 제외 | §1 흑자도산 |
| 한국공인회계사회, 2025년 공동주택 감리 지적사례 공개 | 제외 | §2 공동주택 |
| 한국회계기준원, 제149회 KAI Forum 개최 | 제외 | §4 Forum |
| IASB 공개초안 '위험경감회계' 검토의견 조회 기한 연장 | 해외기준 분류 (안 A) 또는 제외 (안 B) | §3 |
| **A사, 재고자산 저가법 오류로 감리 지적** | **통과** | §1-2 제조업 예외 |
| **IASB 리스 기준 개정, K-IFRS 반영 시기는** | **통과** | §3-4 국내 도입 |
| **회계기준위원회 제12차 회의, 제1116호 개정 의결** | **통과** | §4 강한 신호 |
| **재무제표 중점심사 회계이슈 사전예고** | **통과** | 정상 |
| **법인세법 시행령 개정안 입법예고** | **통과** | 정상 |

---

# 7. 실행 후 검증

## 7-1. 보고 항목

1. 단계별 제외 건수
2. 카테고리별 최종 건수 (공식 / 뉴스 분리)
3. **각 카테고리 전체 항목 제목** — 건수가 적으므로 전량 표시
4. `docs/EXCLUDED_LOG.md`의 신규 제외 항목 중 오제외 의심 목록

## 7-2. 과다 필터링 경고

이번 보완서로 필터가 상당히 촘촘해진다. 다음을 반드시 점검할 것.

- `상장`, `인수`, `합병` 등이 **회계기준 관련 기사**를 잘못 걸러내지 않는가
  (예: "사업결합 회계처리 기준 개정" — `합병`은 없으나 유사 사례 주의)
- `농협`, `수협` 제외가 **일반 세법 개정 기사**를 걸러내지 않는가
- 카테고리별 최종 건수가 3건 미만이면 **즉시 중단하고 보고**할 것

**과소 필터링보다 과다 필터링이 위험하다.** 걸러진 항목은 화면에 나타나지 않아 발견할 수 없다.

---

# 8. Claude Code에 입력할 지시

```
SPEC-ADDENDUM-7.md 읽어줘.

특정 기업 개별 소식(두나무 상장, 삼전닉스 시황), 업종 특화 감리(공동주택),
행사 안내(KAI Forum)를 제외한다. 단 제조업 회계기준 관련 내용은 살린다.

§3(해외 기준)은 안 A로 해줘 — IASB/ISSB는 "해외기준" doc_type으로 분류해서
기본 조회에서만 빼고, EFRAG/FASB/SEC는 지금처럼 완전 제외 유지.

§1부터 순서대로 하고, 각 단계 제외 목록 보여줘.
카테고리별 3건 미만 되면 멈추고 알려줘.
```
