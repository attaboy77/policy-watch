# -*- coding: utf-8 -*-
"""단일 진실 공급원(Single Source of Truth).

카테고리별 키워드, 노이즈 키워드, 출처 신뢰도 등 모든 수집 정책값은
이 파일에서만 정의한다. 다른 모듈에 키워드 문자열을 하드코딩하지 말 것.
"""

CATEGORIES = {
    "kifrs": {
        "label": "K-IFRS", "team": "회계팀", "color": "#1e3a8a",
        # SPEC-ADDENDUM-6.md §2: "금융위원회"/"금융감독원"을 매칭 키워드에서 완전히
        # 제거한다 — 기관명 매칭은 회계 규정이 아닌 금융업 규제(대부업법·감독규정
        # 개정 등)까지 구조적으로 끌어온다(§0-1 실측: K-IFRS 탭 오탐 3건 전부 이
        # 원인). ADDENDUM-5 §4-4의 required_weak+weak_context(회계 문맥) 조합으로도
        # "감독규정"처럼 weak_context 단어가 우연히 함께 있으면 못 걸렀다.
        # 대신 required_weak를 회계 "내용" 키워드로, weak_context를 규제 변경
        # "행위" 키워드로 바꿔 "이 발표가 회계기준에 대한 제도 변경인가"를 직접 본다.
        # required_strong 하나라도 있으면 확정, required_weak는 weak_context와 함께여야
        # 인정(`_utils._has_required_match()` 참고).
        # "외부감사 및 회계"(공백 포함 구절)를 required_strong에 추가 — 아래 참고.
        "required_strong": ["K-IFRS", "한국채택국제회계기준", "회계기준원", "회계기준위원회",
                            "회계처리기준", "회계감리", "감리 지적", "기준서 제",
                            "외부감사 및 회계"],
        # ADDENDUM-6 §8 통과 예시("금융위, 외부감사 및 회계 등에 관한 규정 개정" —
        # 외감법 시행세칙의 정식 명칭)가 addendum 원안 목록(회계기준/회계처리/
        # 재무제표/주석 공시)만으로는 전혀 안 걸려 제외되는 걸 실측으로 확인.
        # **주의**: 처음엔 바로 위 required_weak에 공백 없는 "회계"·"외부감사"를
        # 추가했었는데, 이 리스트가 `build_google_query()`의 OR 검색어에도 그대로
        # 쓰인다는 걸 놓쳐 실제 수집 쿼리를 망가뜨렸다(실측: "조희대 대법원장
        # 부친상…외부 조문 사양" 같은 무관 기사가 Google 검색에서 "외부감사"의
        # 부분/느슨한 매칭으로 대거 잡혀 kifrs 원본 100건 중 사실상 전부가 노이즈가
        # 됨). 공백 없는 한 단어(특히 "회계"처럼 흔한 2글자)는 quote 없이 그대로
        # 검색어에 들어가 Google이 느슨하게 매칭하므로 위험 — 대신 공백 있는 구절
        # "외부감사 및 회계"는 자동으로 quote되어(`q()` 참고) 정확한 문구 매칭만
        # 되므로 required_strong으로 옮겼다(2026-08-31, 실측으로 확인 후 수정).
        "required_weak": ["회계기준", "회계처리", "재무제표", "주석 공시"],
        "weak_context": ["개정", "제정", "공표", "의결", "공개초안", "적용지침", "질의회신"],
        "combine":  ["개정", "제정", "공표", "MPM", "경영진성과측정치",
                     "기준서", "주석 공시", "회계처리 지침", "감리 지적사례"],
        # 네이버 API는 (A OR B) AND (C OR D) 구문을 지원하지 않음 → 단순 질의를 나열
        "naver_queries": ["K-IFRS 개정", "회계기준 개정", "회계기준원 공표",
                          "회계감리 지적사례", "회계처리 지침"],
    },
    "tax": {
        "label": "세법", "team": "세무팀", "color": "#047857",
        # SPEC-ADDENDUM-6.md §2-3: "국세청"/"기획재정부" 기관명을 제거한다. 세목
        # 화이트리스트(TAX_SUBJECTS/match_tax_subject)가 이미 L3 단계에서 관련성을
        # 걸러주므로 기관명 없이도 안전하다 — 반면 기관명을 남겨두면 "국세청, 선진
        # 세정경험 수출"류 홍보 기사까지 세법 카테고리로 끌어온다(§3에서 별도 처리).
        "required": ["세법", "세제개편", "유권해석"],
        # "세무조사"는 여기 없다 — 단독 매칭 시 검찰 수사·압수수색 기사를 끌어오는 게
        # 확인돼(SPEC-ADDENDUM-3.md §5-2) TAX_INVESTIGATION_COMBO로 따로 뺐다.
        "combine":  ["개정안", "시행령 개정", "조세특례제한법", "부가가치세법",
                     "법인세법", "예규", "판례", "조세심판원"],
        "naver_queries": ["법인세법 개정", "부가가치세법 개정", "세제개편안",
                          "국세청 유권해석", "조세심판원 결정"],
    },
    "icfr": {
        "label": "내부회계", "team": "내부회계관리팀", "color": "#b45309",
        # SPEC-ADDENDUM-5.md §4: "내부통제"는 군사·보안·경영 전반의 일반 명사라
        # 단독 매칭하면 이란 군부 기사까지 들어온다(실측 확인). required_strong
        # 하나라도 있으면 확정, required_weak(내부통제류)는 weak_context(회계 문맥)와
        # 함께 있을 때만 인정한다.
        "required_strong": ["내부회계관리제도", "내부회계관리", "연결내부회계",
                            "외감법", "외부감사법", "주식회사 등의 외부감사에 관한 법률"],
        "required_weak": ["내부통제", "내부 통제"],
        "weak_context": ["회계", "재무제표", "공시", "감사인", "외부감사", "재무보고",
                         "감사위원회", "회계법인", "평가·보고", "모범규준", "K-IFRS"],
        "combine":  ["감사 전환", "모범규준", "취약점", "외감법 개정", "연결 내부회계",
                     "평가 가이드라인", "내부통제 미비점", "설계·운영", "감사인 검토"],
        "naver_queries": ["내부회계관리제도", "연결 내부회계", "외감법 개정",
                          "내부회계 모범규준"],
    },
    "esg": {
        "label": "ESG", "team": "ESG팀", "color": "#0e7490",
        # SPEC-ADDENDUM-6.md §2-3에 "esg: 금융위원회 제거" 항목이 있으나, 현재
        # required/combine 어디에도 "금융위원회"가 없어 이미 해당 없음(조치 불필요).
        "required": ["ESG 공시", "지속가능경영", "ISSB", "KSSB", "GRI"],
        "combine":  ["의무화", "공시기준", "로드맵", "스코프3", "Scope 3",
                     "공급망 실사", "글로벌 스탠다드", "가이드라인"],
        "naver_queries": ["ESG 공시 의무화", "KSSB 공시기준", "ISSB 기준",
                          "지속가능성 공시", "공급망 실사"],
    },
}

NOISE_KEYWORDS = [
    "테마주", "수혜주", "주가", "급등", "상한가", "매수 리포트", "특징주",
    "개미", "재테크", "비트코인", "투자 유망", "증시 일정", "목표주가", "상승률",
]

# ── 논의자료(TF·실무그룹 중간 산출물) 판정 (2026-08-28 사용자 피드백) ───────
# "제1118호 정착지원 TF 논의 내용(4차)"처럼 기준서 확정 전 실무 TF의 중간
# 논의 기록은 실무에 쓸 게 없다 — 별도 doc_type("논의자료")로 분류해 기본
# 조회/오늘의 정책동향에서 뺀다(상설자료와 같은 처리 방식, `is_static` 참고).
DISCUSSION_MATERIAL_KEYWORDS = [
    "TF", "태스크포스", "논의 내용", "회의 결과", "진행 경과", "중간 보고",
    "검토 경과", "워킹그룹", "실무그룹",
]
# 아래 중 하나라도 제목에 함께 있으면 확정된 결론물이므로 논의자료로 재분류하지
# 않고 원래 doc_type_of() 판정 결과를 그대로 둔다(사용자 지시).
DISCUSSION_OVERRIDE_KEYWORDS = ["의결", "공표", "제정", "개정", "확정"]

# ── 조직 운영성 공지 제외 (SPEC-ADDENDUM-4.md §1) ───────────────────────────
# 기관의 인사·조직·운영 공지. 규제 내용과 무관하므로 **계층(tier) 무관하게 전부
# 제외한다** — L1(공식기관)도 NOISE_KEYWORDS는 면제받지만 이 목록은 면제받지 않는다.
# "간담회", "회의" 처럼 실질 내용이 섞인 단어는 통째로 넣지 않는다(§1 주의사항).
ADMIN_NOISE_KEYWORDS = [
    # 인사·조직
    "인사보도", "인사발령", "전보", "승진", "임명", "취임", "이임", "퇴임",
    "조직개편", "직원 채용", "채용 공고", "인턴 모집",
    # 기관 운영
    "청사 이전", "시스템 점검", "서비스 점검", "홈페이지 점검", "임시 휴무",
    "정기 휴관", "본인 인증", "이용 안내", "접속 장애",
    # 행사·의례
    "시무식", "종무식", "기념식", "체육대회", "봉사활동", "간담회 개최 안내",
    # 기관 홍보·외교 활동 (SPEC-ADDENDUM-6.md §3): 규제 변경이 아니라 기관의
    # 대외 활동 홍보. "국제조세"·"조세조약"·"글로벌최저한세" 자체는 실무 관련성이
    # 높으므로 여기 넣지 않는다 — 아래는 "활동 자체를 알리는 표현"만 담았다.
    # 예: "글로벌최저한세, 한국 국세청의 선진 세정경험 수출"(§0-1 실측 오탐).
    "세정경험", "경험 수출", "우수사례 공유", "국제협력", "기술협력",
    "초청 연수", "방한", "방문단", "업무협약 체결", "양해각서", "공동선언",
    "국제기구 진출", "수상", "표창",
    # 행사·포럼 안내 (SPEC-ADDENDUM-7.md §4): "포럼 개최"는 이미 CORPORATE_PR_
    # KEYWORDS에 있지만 그건 L3 전용이라 L1(예: KASB 자체 포럼 공지)이 못 걸렀다.
    # "Forum"이 영문이라 기존 키워드들과 안 겹쳐 추가했다(§4-1 실측: "제149회
    # KAI Forum" 미탐지).
    "Forum", "Conference", "Seminar", "Symposium", "Workshop",
    "컨퍼런스", "심포지엄", "워크숍", "웨비나",
    "개최 안내", "참가 신청", "사전 등록", "발표자 모집",
]

# ── 적용 대상 판정 게이트 (SPEC-ADDENDUM-6.md §1) ───────────────────────────
# 기존 필터는 "이게 회계·세무 주제인가"를 본다. 이 게이트는 다른 질문을 본다:
# "이 규제의 적용 대상에 우리(팜한농 — 비금융 일반 제조법인, 외부감사 대상,
# 연결 실체)가 포함되는가?" — 주제가 맞아도 대상이 아니면 제외한다(§0-2).
# **L1/L2/L3 전 계층에 적용**한다 — 기존 필터들과 달리 공식 소스도 면제하지
# 않는다(§1-2). 금융위가 낸 공식 자료라도 금융업 대상이면 우리와 무관하다.
#
# §8 예외 케이스: "EU CSRD 국내 도입 영향…금융위 검토 착수"처럼 해외 관할이라도
# 국내 도입/적용 맥락이 함께 있으면 통과시킨다(foreign_exception_context).
# TODO(§8 설정파일화, 미착수): 이 값들은 운영하며 계속 조정될 것이므로 최종적으로
# `data/filters.yml`로 옮기는 게 맞다 — 지금은 다른 카테고리 키워드처럼 여기 둔다.
APPLICABILITY = {
    "excluded_entities": {
        # 금융업 전용 규제 — 우리는 금융회사가 아니다.
        "financial": [
            "은행법", "보험업법", "대부업", "대부채권", "신협", "새마을금고",
            "저축은행", "상호금융", "여신전문금융", "카드사", "캐피탈",
            "자산운용사", "증권사", "금융투자업", "전자금융업자",
            "금융복합기업집단", "부실채권", "NPL", "지급여력", "BIS비율",
            "예금보험", "가상자산사업자",
            "감독규정",  # 금융위 감독규정 = 금융업 대상(§0-1 실측 오탐)
        ],
        # 공공기관·지자체 고유
        # "지방재정"은 뺐다(2026-08-31 사용자 지시) — 지방소비세 보전 규정처럼
        # 실제로는 지방세법 개정 내용이라 일반법인에도 적용될 수 있는 항목까지
        # 걸러버린 오제외 사례가 실측 확인됨(docs/EXCLUDED_LOG.md, "박용갑 의원,
        # 지방세법 개정안…지방재정 7조 원" 2건). §9-2/§10 원칙대로 애매하면
        # 통과시키는 쪽으로 전환.
        "public": [
            "공기업", "준정부기관", "지방공기업", "출자출연기관", "산하기관",
            "경영평가",
        ],
        # 비영리·특수법인 전용
        "nonprofit": [
            "사회복지법인", "의료법인", "학교법인", "종교단체", "비영리법인",
            "공익법인",
        ],
        # 중소기업 특례 — 우리는 외부감사 대상 상장·연결 실체.
        "smb_only": [
            "일반기업회계기준", "중소기업회계기준", "소상공인", "간이과세",
            "개인사업자",
        ],
        # 업종 특화 감리·회계 (SPEC-ADDENDUM-7.md §2) — 회계 주제·업종 필터를
        # 통과하지만 우리와 무관한 특수 회계 영역. "조합" 단독은 넣지 않는다
        # ("노동조합"·"조합원" 등에서 오탐 발생, §2-2 주의사항).
        "industry_specific": [
            # 주택·건설
            "공동주택", "아파트 회계", "관리사무소", "입주자대표", "재건축조합",
            "재개발조합", "정비사업조합", "주택조합",
            # 협동조합·상호금융(금융 scope의 "신협"/"상호금융"과 별개 — 업종 자체가
            # 다른 협동조합들)
            "농협", "수협", "산림조합", "생협",
            # 기타 특수 회계
            "학교회계", "유치원 회계", "병원회계", "의료기관 회계", "사학기관",
            "국가회계", "지방자치단체 회계", "정당 회계", "정치자금",
        ],
    },
    # 해외 관할. foreign_exception_context가 함께 있으면 예외적으로 통과한다.
    # IASB/ISSB는 여기 없다 — §3(안 A)에 따라 완전 제외 대신 doc_type="해외기준"
    # 분류로 처리한다(FOREIGN_STANDARD_BODIES 참고).
    "foreign_jurisdiction": [
        "EFRAG", "ESRS", "CSRD", "CSDDD", "SEC 기후공시", "FASB", "US GAAP",
        "미국 회계기준", "EU 택소노미",
    ],
    # ADDENDUM-7 §3-4: IASB/ISSB의 "국내 도입 논의는 항상 통과"에도 이 목록을
    # 그대로 재사용한다(is_foreign_standard() 참고) — "K-IFRS 반영"/"한국 도입"/
    # "도입 로드맵" 추가.
    "foreign_exception_context": [
        "국내 도입", "국내 적용", "국내 영향", "한국 기업", "국내 기업 대응",
        "KSSB", "금융위", "K-IFRS 반영", "한국 도입", "도입 로드맵",
    ],
}

# ── 규제성 판정 게이트 (SPEC-ADDENDUM-5.md §1) ──────────────────────────────
# L3(뉴스)는 "제도가 바뀌었다"는 내용일 때만 통과시킨다. L1/L2(공식 소스)는
# 게이트 미적용 — 기관 발표는 그 자체가 규제 정보이므로.
REGULATORY_SIGNALS = [
    # 입법·제정 절차
    "개정", "제정", "공포", "시행", "입법예고", "의결", "공표", "확정",
    "개정안", "제정안", "공개초안", "의견수렴", "의견조회", "심의",
    # 기준·지침
    "기준서", "모범규준", "가이드라인", "적용지침", "해석", "지침",
    "질의회신", "예규", "유권해석", "집행기준",
    # 제도 도입·적용
    "의무화", "도입", "적용 대상", "적용 시기", "유예", "면제", "완화", "강화",
    "로드맵", "단계적 적용",
    # 발표 주체 행위
    "발표", "배포", "발간", "안내", "고시",
]

# ── 타사 홍보성 보도 제외 (SPEC-ADDENDUM-5.md §3) ───────────────────────────
# 특정 기업의 자체 활동 소식(협약·수상·행사 개최 등)은 규제 정보가 아니다.
CORPORATE_PR_KEYWORDS = [
    "가동", "출범", "발족", "킥오프", "맞손", "협약", "MOU", "업무협약",
    "앞장", "박차", "총력", "선포", "다짐", "결의",
    "수상", "선정", "인증 획득", "달성", "돌파", "성과",
    "구축", "고도화", "확대", "강화 나서", "추진", "도입키로",
    "간담회 개최", "세미나 개최", "포럼 개최", "설명회 개최",
]
# 홍보 동사가 있어도 이 중 하나(제도 변경 절차를 나타내는 강한 신호)가 함께
# 있으면 홍보성이 아니라고 본다.
CORPORATE_PR_STRONG_SIGNALS = ["개정", "제정", "공포", "입법예고", "공개초안",
                              "기준서", "모범규준", "질의회신", "예규", "의무화"]

# ── 개별 기업 소식 제외 (SPEC-ADDENDUM-7.md §1) ─────────────────────────────
# 회계 주제이고 업종 필터도 통과하지만, 규제가 아니라 "한 회사의 개별 사정"인
# 기사(자본시장 이벤트·실적·시황)를 제외한다. L3 전용(§5 처리순서 7번).
# "상장"/"비상"은 여기 없다 — 각각 "상장사"/"상장기업"/"비상장회사"(기업 상태를
# 가리키는 일반 명사) 부분일치로 관련 K-IFRS/외감법 기사를 오탐 제외하는 걸 실측
# 확인(2026-08-31, "상장사 감사의견... 새로운 K-IFRS 도입", "신외감법 시행으로
# 상장사 규제비용 증가" 등). `_utils._LISTING_EVENT_RE`/`_EMERGENCY_RE`가 부정
# 전방탐색으로 대신 처리한다("상장 확정"·"상장 추진" 등 실제 이벤트는 그대로 잡음).
COMPANY_EVENTS = [
    # 자본시장 이벤트
    "상장폐지", "재상장", "IPO", "공모", "청약", "유상증자", "무상증자",
    "자사주", "지분 매각", "인수", "합병", "M&A", "분할", "지주회사 전환",
    # 실적·전망
    "실적 발표", "어닝", "컨센서스", "영업이익", "매출 증가", "흑자 전환",
    "적자 전환", "어닝 서프라이즈", "목표가",
    # 재무 위기
    "흑자도산", "부도", "워크아웃", "법정관리", "회생절차", "자본잠식", "상장 적격성",
    # 시황·전망 어투
    "공포", "우려 확산", "초비상", "위기감", "술렁",
]
# is_company_event()가 True여도 이 중 하나(제도 변경 절차)가 있으면 제외하지 않는다.
# ADDENDUM-7 §1-1 원안엔 "공포"가 있었으나 뺐다 — "공포"는 위 COMPANY_EVENTS
# "시황·전망 어투"에도 있는 동형이의어(법령 공포/시장 공포)라, "…흑자도산 공포"
# 같은 패닉 헤드라인이 자기 자신을 강한 신호로 오인해 통과해버리는 실측 버그가
# 있었다(2026-08-31 발견). "공포"(법령) 없이도 "개정"/"제정"/"입법예고" 등이
# 이미 법령 공포 문맥을 충분히 잡는다.
# "K-IFRS"도 강한 신호에 추가했다(2026-08-31 실측) — "[K-IFRS 제1118호 시리즈]
# 두 개의 영업이익"처럼 기준서 해설 기사가 "영업이익"(COMPANY_EVENTS) 하나로
# 개별 기업 실적 기사로 오탐 제외되는 걸 확인. K-IFRS가 언급되면 그 자체로
# 회계기준 해설/적용 문맥일 가능성이 높다.
COMPANY_EVENT_STRONG_SIGNALS = ["개정", "제정", "입법예고", "공개초안",
                                "기준서", "모범규준", "가이드라인", "질의회신",
                                "예규", "의무화", "시행령", "K-IFRS"]
# §1-2: 개별 기업 사례라도 제조업 회계기준과 직결되면 통과시킨다(실무 참고
# 가치가 크다는 사용자 요청 반영) — is_company_event()가 True여도 이 문맥이
# 있으면 False로 뒤집는다.
MANUFACTURING_ACCOUNTING_CONTEXT = [
    "재고자산", "재고 평가", "저가법", "유형자산", "손상차손", "감가상각",
    "제조원가", "원가 배분", "매출원가", "수익인식", "진행기준", "리스회계",
    "개발비", "연구개발비 자산화", "충당부채", "복구충당부채", "환경충당", "배출권",
]

# ── 행사·포럼 안내 제외 보강 (SPEC-ADDENDUM-7.md §4) ────────────────────────
# 영문 행사 표현(Forum 등)은 위 ADMIN_NOISE_KEYWORDS에 이미 추가했다.
# "제N회"/"제N차" 패턴 + 아래 강한 신호가 없으면 행사 안내로 본다(§4-2,
# is_event_announcement() 참고).
EVENT_ANNOUNCEMENT_STRONG_SIGNALS = ["개정", "제정", "공표", "의결", "확정", "공개초안 발표"]

# ── 해외 기준 처리 — 안 A: 별도 분류 (SPEC-ADDENDUM-7.md §3) ────────────────
# IASB/ISSB는 K-IFRS/KSSB의 원천이라 완전 제외하지 않고 doc_type="해외기준"으로
# 수집은 하되 기본 조회/오늘의 정책동향에서만 뺀다(§3-2 안 A, 사용자 지시
# 2026-08-31 "§3은 안 A로 해줘"). EFRAG/FASB/SEC 등은 ADDENDUM-6
# APPLICABILITY.foreign_jurisdiction의 완전 제외를 그대로 유지한다(§3-2).
# 안 B(전면 제외)로 전환하려면 이 리스트를 비우고 foreign_jurisdiction에
# "IASB"/"ISSB"를 추가하면 된다(§3-3).
FOREIGN_STANDARD_BODIES = ["IASB", "ISSB"]

TRUST_TIERS = [
    (1, 100, {
        "kasb.or.kr": "한국회계기준원", "fsc.go.kr": "금융위원회",
        "law.go.kr": "국가법령정보센터", "nts.go.kr": "국세청",
        "moef.go.kr": "기획재정부", "mofe.go.kr": "기획재정부",  # moef.go.kr이 301로 리다이렉트되는 실제 도메인(SOURCE_PROBE.md D1)
        "fss.or.kr": "금융감독원", "kicpa.or.kr": "한국공인회계사회",
        "k-icfr.org": "내부회계관리제도운영위원회",
    }),
    (2, 80, {
        "intn.co.kr": "일간NTN", "taxtimes.co.kr": "한국세정신문",
        "joseilbo.com": "조세일보", "impacton.net": "임팩트온",
        "esgeconomy.com": "ESG경제", "cpanews.co.kr": "CPA뉴스",
    }),
    (3, 70, {
        "samil.com": "삼일회계법인", "pwc.com": "PwC",
        "kr.kpmg.com": "삼정KPMG", "kpmg.com": "KPMG",
        "samjong.co.kr": "삼정KPMG", "ey.com": "EY한영",
        "deloitte.com": "딜로이트", "deloitte.kr": "딜로이트 안진",
        "anjin.co.kr": "딜로이트 안진",
    }),
    (4, 50, {
        "mk.co.kr": "매일경제", "hankyung.com": "한국경제",
        "sedaily.com": "서울경제", "edaily.co.kr": "이데일리",
        "fnnews.com": "파이낸셜뉴스", "mt.co.kr": "머니투데이",
    }),
]
DEFAULT_TIER, DEFAULT_TRUST = 5, 20

COLLECT_WINDOW_DAYS = 90     # 수집 기간

# ── 계층별 상한 (SPEC-ADDENDUM.md §1) ───────────────────────────────────────
# L1(공식 원문)/L2(공식 보도자료)는 무조건 노출·노이즈 필터 면제·상한 미적용.
# L3(뉴스)만 상한을 적용한다 — 기존 MAX_ITEMS_PER_CATEGORY(60)를 대체.
MAX_NEWS_PER_CATEGORY = 15      # L3 카테고리당 상한(기존 60 → 15)
MAX_TIER4_PER_CATEGORY = 5      # L3 중 tier4(종합 경제지)는 카테고리당 최대 5건
MAX_OFFICIAL_PER_CATEGORY = None  # L1/L2는 상한 없음(그대로 전량 노출)

# ── 유사 기사 중복 제거 (SPEC-ADDENDUM-4.md §3, SPEC-ADDENDUM-5.md §5로 개선) ─
# L3(뉴스)에만 적용. SIMILARITY_THRESHOLD는 ADDENDUM-5 §5-3에서 0.65→0.55로
# 낮추고, 대신 주체(§5-3 extract_subject) 일치 조건(b)으로 오탐을 방지한다.
SIMILARITY_THRESHOLD = 0.55
SUBJECT_SIMILARITY_THRESHOLD = 0.35  # 조건(b): 주체 일치 + 이 임계값 이상이면 병합
SIMILARITY_DAY_WINDOW = 3  # 조건(b) 전용 — 주체가 같아도 3일 넘게 떨어져 있으면 별개로 본다

# ── 공식-뉴스 연결 (SPEC-ADDENDUM-4.md §4) ──────────────────────────────────
RELATED_NEWS_MAX = 3
RELATED_NEWS_DAY_WINDOW = 7
RELATED_NEWS_MIN_SIMILARITY = 0.30

# ── 문서 종류 판정 (SPEC-ADDENDUM-2.md §2-1, §4) ───────────────────────────
# 앞쪽 규칙이 우선한다(순서 중요). doc_type_of()가 이 순서대로 제목을 검사한다.
DOC_TYPE_RULES = [
    ("공개초안",      ["공개초안", "공개 초안", "개정안", "제정안", "입법예고", "(안)", "의견조회", "의견수렴"]),
    ("검토의견",      ["검토의견", "의견서", "의견수렴 결과", "코멘트"]),
    ("적용지침",      ["적용지침", "시행지침", "평가·보고 지침", "평가 및 보고", "적용 가이드", "가이드라인"]),
    ("모범규준",      ["모범규준", "설계·운영", "설계 및 운영"]),
    ("감사·검토기준", ["감사기준", "검토기준", "감사인", "검토·감사"]),
    ("질의회신",      ["질의회신", "질의 회신", "유권해석", "예규", "서면질의", "질의응답"]),
    ("FAQ",          ["FAQ", "자주 묻는", "Q&A"]),
    ("예시서식",      ["예시", "서식", "템플릿", "양식", "샘플"]),
    ("로드맵·일정",   ["로드맵", "도입 일정", "적용 일정", "단계별 적용", "시행 일정"]),
    ("해설·교육자료", ["해설", "교육", "설명회", "안내서", "핸드북", "실무"]),
    ("결정례·판례",   ["결정례", "판례", "심판례", "조세심판"]),
    ("제·개정",       ["제정", "개정", "공표", "의결", "확정"]),
    ("보도자료",      ["보도자료", "보도참고", "발표"]),
]

# ── 세법 카테고리 노이즈 보강 (SPEC-ADDENDUM.md §5, L3에만 적용) ────────────
INCIDENT_KEYWORDS = [
    "검찰", "수사", "압수수색", "구속", "기소", "혐의", "고발",
    "로비", "비리", "은닉", "포탈", "탈세 혐의", "구형", "선고",
]
# INCIDENT_KEYWORDS가 걸려도 이 중 하나가 같이 있으면 "제도 관련 기사"로 보고 살린다
# (예: "탈세 혐의 판결에 따른 예규 변경" — 단순 배제가 아니라 AND 조건인 이유).
PROCEDURAL_KEYWORDS = ["개정", "시행령", "예규", "지침"]

# "세무조사"는 단독으로 두면 검찰 수사·압수수색 기사를 끌어온다(SPEC-ADDENDUM-3.md §5-2).
# 아래 조합 중 하나가 같이 있을 때만 매칭한다.
TAX_INVESTIGATION_COMBO = {
    "trigger": "세무조사",
    "qualifiers": ["사전통지", "대상 선정", "운영규정", "절차", "기간 연장", "납세자권리"],
}

# ── 세목 화이트리스트 (SPEC-ADDENDUM-3.md §2) ───────────────────────────────
TAX_SUBJECTS_YML_PATH = "data/tax_subjects.yml"


def _load_tax_subjects(path: str = TAX_SUBJECTS_YML_PATH):
    """data/tax_subjects.yml을 읽어 (subjects, features)를 반환한다.

    파일이 없거나 파싱에 실패하면 **전체 세목 통과**로 동작하도록 빈 리스트를
    반환하고 경고를 남긴다(ADDENDUM-3 §2-1: "조용히 0건이 되는 것보다 낫다").
    `subjects`가 빈 리스트면 `match_tax_subject()`가 전부 통과시키는 걸로 약속한다.
    """
    try:
        import yaml
        with open(path, encoding="utf-8") as f:
            data = yaml.safe_load(f) or {}
        subjects = [s for s in data.get("subjects", []) if s.get("enabled", True)]
        features = data.get("features", {})
        label = ", ".join(s["label"] for s in subjects) or "(없음)"
        print(f"[tax] 활성 세목 {len(subjects)}개: {label}")
        return subjects, features
    except FileNotFoundError:
        print(f"[tax] {path} 없음 — 세목 화이트리스트 없이 전체 세목 통과로 동작합니다.")
        return [], {}
    except Exception as exc:  # noqa: BLE001 - YAML 파싱 실패 등, 조용히 죽는 것보다 낫다
        print(f"[tax] {path} 파싱 실패({exc}) — 전체 세목 통과로 동작합니다.")
        return [], {}


TAX_SUBJECTS, TAX_FEATURES = _load_tax_subjects()
