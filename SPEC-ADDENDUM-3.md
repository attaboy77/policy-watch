# SPEC 보완서 3 — 세법 카테고리 세목 관리
## Policy Watch v1.3 (SPEC-ADDENDUM-2.md의 §3-④를 대체)

> **적용 방법**
> `C:\policy-watch\SPEC-ADDENDUM-3.md` 로 저장 후 Claude Code에 입력:
> `SPEC-ADDENDUM-3.md 읽고 Phase 3A 진행해줘`
>
> 이 문서는 SPEC-ADDENDUM-2.md의 **§3-④ 세법 표만 대체**한다. 나머지(doc_type, stage, 판정 규칙)는 그대로 유효하다.

---

# 1. 핵심 원칙: 회사 관련 세목만 수집

Phase 2 실측 결과, 세법 카테고리 상위 5건 중 3건이 종합부동산세 기사였다. 법인 실무와 무관하다. 세목 화이트리스트를 도입해 이를 근본적으로 차단한다.

**수집 대상 세목(초기 기본값)**

| 키 | 세목 | 근거 법령 |
|---|---|---|
| `corp` | 법인세 | 법인세법 |
| `vat` | 부가가치세 | 부가가치세법 |
| `wht` | 원천세 | 소득세법 중 원천징수 관련, 법인세법 원천징수 |
| `intl` | 국제조세 및 이전가격 | 국제조세조정에 관한 법률 |
| `stt` | 조세특례제한법 | 조세특례제한법 |
| `local` | 지방세 | 지방세법, 지방세특례제한법 |

**명시적 제외(기본값)**
종합부동산세, 상속세·증여세, 양도소득세, 종합소득세(개인), 개별소비세, 주세, 관세

제외 목록은 하드코딩하지 않는다. 화이트리스트에 없으면 자동으로 빠지는 구조로 만든다.

---

# 2. 세목 설정 파일 (`data/tax_subjects.yml`)

세목 추가·제외는 **코드를 건드리지 않고 이 파일만 수정**해서 가능해야 한다. 관리자(회계팀장)가 직접 열어 수정할 수 있도록 YAML로 둔다.

```yaml
# 세법 카테고리 수집 대상 세목
# enabled를 false로 바꾸면 해당 세목은 수집에서 제외된다.
# 새 세목을 추가하려면 아래 형식대로 블록을 하나 더 쓰면 된다.

subjects:
  - key: corp
    label: 법인세
    enabled: true
    keywords: [법인세, 법인세법, 각사업연도소득, 법인지방소득세]
    laws: [법인세법, 법인세법 시행령, 법인세법 시행규칙]

  - key: vat
    label: 부가가치세
    enabled: true
    keywords: [부가가치세, 부가세, 부가가치세법, 세금계산서, 매입세액, 영세율, 면세]
    laws: [부가가치세법, 부가가치세법 시행령, 부가가치세법 시행규칙]

  - key: wht
    label: 원천세
    enabled: true
    keywords: [원천징수, 원천세, 지급명세서, 연말정산, 간이세액표]
    laws: [소득세법, 소득세법 시행령]

  - key: intl
    label: 국제조세·이전가격
    enabled: true
    keywords: [국제조세, 이전가격, 정상가격, 과소자본, 글로벌최저한세, 필라2, BEPS, 조세조약]
    laws: [국제조세조정에 관한 법률, 국제조세조정에 관한 법률 시행령]

  - key: stt
    label: 조세특례제한법
    enabled: true
    keywords: [조세특례제한법, 조특법, 세액공제, 통합투자세액공제, 연구인력개발비, R&D 세액공제]
    laws: [조세특례제한법, 조세특례제한법 시행령]

  - key: local
    label: 지방세
    enabled: true
    keywords: [지방세, 취득세, 재산세, 지방소득세, 주민세, 지방세특례제한법]
    laws: [지방세법, 지방세법 시행령, 지방세특례제한법]

  # 필요 시 아래 주석을 해제해 사용
  # - key: excise
  #   label: 개별소비세
  #   enabled: false
  #   keywords: [개별소비세]
  #   laws: [개별소비세법]
```

## 2-1. 구현 요구사항

- `sources/_config.py`가 기동 시 이 파일을 읽어 `TAX_SUBJECTS`로 로드한다.
- 파일이 없거나 파싱 실패 시 **전체 세목 통과**로 동작하고 경고 로그를 남긴다. (조용히 0건이 되는 것보다 낫다)
- `enabled: false`인 세목의 키워드는 매칭에 사용하지 않는다.
- 로드 결과를 기동 로그에 출력한다: `[tax] 활성 세목 6개: 법인세, 부가가치세, 원천세, 국제조세·이전가격, 조세특례제한법, 지방세`

---

# 3. 세목 필터 적용 범위 — 계층별로 다르다

**중요:** 세목 필터를 모든 소스에 일괄 적용하면 안 된다. 기재부 세제개편안은 전 세목을 한 문서로 다루기 때문에, 특정 세목 키워드가 제목에 없다는 이유로 버려질 수 있다.

| 계층 | 세목 필터 | 이유 |
|---|---|---|
| **L1 기재부 세제개편안, 국세청 개정세법 해설** | **면제** | 전 세목을 포괄하는 종합 문서. 무조건 수집 |
| **L1 법제처 법령** | **화이트리스트 방식** | `laws:` 목록에 있는 법령명만 수집 (가장 정확) |
| **L2 정책브리핑 보도자료** | 적용 | 제목·본문에 활성 세목 키워드가 있어야 통과 |
| **L3 뉴스** | **엄격 적용** | 종부세 등 노이즈의 주 발생원 |

## 3-1. 판정 함수

```python
def match_tax_subject(text: str) -> list[str]:
    """활성 세목 중 매칭되는 것들의 key 리스트를 반환. 없으면 빈 리스트."""
    t = _norm(text)
    hits = []
    for s in TAX_SUBJECTS:
        if not s["enabled"]:
            continue
        if any(_norm(k) in t for k in s["keywords"]):
            hits.append(s["key"])
    return hits


def pass_tax_filter(item: dict) -> bool:
    """계층에 따라 세목 필터 적용 여부를 결정."""
    if item["category"] != "tax":
        return True
    if item.get("_layer") in ("L1_comprehensive",):   # 세제개편안, 개정세법 해설
        return True
    return bool(match_tax_subject(item["title"] + " " + item.get("_body", "")))
```

## 3-2. 스키마 추가

세목 정보를 `items[]`에 기록해 프론트엔드 필터에 활용한다.

```json
{
  "category": "tax",
  "tax_subjects": ["corp", "stt"],
  ...
}
```
- `tax` 카테고리가 아니면 `null`
- 세제개편안처럼 전 세목 포괄 문서는 `["_all"]`
- 프론트엔드: 세법 탭 선택 시 세목 칩 필터 노출 (`법인세` `부가가치세` `원천세` …)

---

# 4. 수집 소스 (세법)

| # | 소스 | doc_type | 세목 필터 | 비고 |
|---|---|---|---|---|
| D1 | 기획재정부 세법개정안 | `공개초안` / `제·개정` | **면제** | 연도별 개정안 및 상세본. `_layer = L1_comprehensive` |
| D2 | 국세청 개정세법 해설 | `해설·교육자료` | **면제** | 연도별 자료집. PDF 링크 위주 |
| D3 | 국가법령정보시스템 (법제처 Open API) | `제·개정` | **화이트리스트** | `laws:` 목록의 법령만. 시행일 추출 필수 |
| D4 | 정책브리핑 (기재부·국세청 보도자료) | `보도자료` | 적용 | 시행령 개정 추진, 입법 후속 절차 |
| D5 | 네이버·구글 뉴스 | `기사` | **엄격 적용** | L3. 상한 15건 |

## 4-1. 법제처 화이트리스트 방식이 중요한 이유

법제처 API는 법령명으로 조회할 수 있다. 키워드 매칭이 아니라 **법령명 목록으로 직접 조회**하면 오탐이 원천적으로 없다. 세법 카테고리에서 가장 정확한 소스가 되므로 우선순위를 높게 둔다.

조회 대상 법령은 `tax_subjects.yml`의 `laws:` 필드를 모두 합친 목록이다. 세목을 끄면 해당 법령도 자동으로 조회 대상에서 빠진다.

---

# 5. MVP 제외 항목 — 예규·판례·심판례

**초기 버전에서는 수집하지 않는다.** 다만 나중에 설정 한 줄로 켤 수 있도록 설계한다.

```yaml
# data/tax_subjects.yml 하단에 추가

features:
  # MVP에서는 false. 안정화 후 true로 전환하면 수집이 시작된다.
  collect_rulings: false      # 국세청 예규·유권해석
  collect_precedents: false   # 판례
  collect_tribunal: false     # 조세심판원 심판례
```

## 5-1. 구현 지침

- `sources/official/nts_rulings.py` 등 어댑터 **파일 자체는 만들되**, `features` 플래그가 `false`면 `fetch()`가 즉시 빈 리스트를 반환하도록 한다.
- Phase 3A 조사 단계에서는 이들 소스도 **접근 가능성만 확인해 기록**해둔다. 나중에 켤 때 다시 조사하지 않아도 되도록.
- `doc_type` 열거값에는 `질의회신`, `결정례·판례`를 그대로 유지한다. (스키마 변경 없이 켤 수 있도록)

## 5-2. 참고

예규·유권해석은 세무 실무에서 활용도가 높은 자료다. 세제개편안은 언론을 통해 자연히 접하게 되지만 예규는 능동적으로 찾지 않으면 놓치기 쉽다. MVP 안정화 후 우선적으로 켜볼 만한 항목이다.

---

# 6. 노이즈 필터 조정

SPEC-ADDENDUM.md §5의 `INCIDENT_KEYWORDS` 규칙은 그대로 유지하되, **세목 화이트리스트가 1차 방어선**이므로 순서를 다음과 같이 둔다.

```
L3 뉴스 처리 순서:
1) 카테고리 분류 (classify)
2) 세목 필터 (match_tax_subject) ← 여기서 종부세·상속세 등이 걸러짐
3) 노이즈 키워드 (is_noise)
4) 사건사고 필터 (INCIDENT_KEYWORDS + 제도 키워드 부재 시 제외)
5) 신뢰도·점수 계산 후 상한 적용
```

2단계가 가장 강력한 필터가 된다.

---

# 7. 테스트 필수 케이스

`tests/test_tax_subjects.py`에 포함할 것.

| 제목 | 기대 결과 | 확인 목적 |
|---|---|---|
| 초고가 주택 종부세 인상, 장특공제 10억원 한도 | **제외** | 종부세는 화이트리스트 밖 |
| 소득·법인세 줄고 종부세 증가…5년간 세수효과 | **통과** (`corp`) | 법인세가 포함되면 통과 |
| 2026년 세제개편안 발표 (기재부, L1) | **통과** (`_all`) | L1 종합 문서는 면제 |
| 부가가치세법 시행령 일부개정령안 입법예고 | **통과** (`vat`) | |
| 상속세 및 증여세법 개정안 | **제외** | 화이트리스트 밖 |
| 통합투자세액공제 확대 (조특법) | **통과** (`stt`) | |
| 글로벌최저한세 신고 안내 | **통과** (`intl`) | |
| `enabled: false`로 지방세를 끈 뒤 "취득세 개편" | **제외** | 설정 반영 확인 |

---

# 8. Claude Code에 입력할 지시

```
SPEC-ADDENDUM-3.md 읽어줘.

세법은 회사 관련 세목만 수집한다. 세목 목록은 data/tax_subjects.yml에서
관리자가 직접 켜고 끌 수 있어야 한다.
예규·판례·심판례는 MVP에서 제외하되 설정으로 켤 수 있게 설계한다.

Phase 3A부터 진행해줘.
```
