# SPEC 보완서 8 — 검색 기능·캘린더 정렬·AI 요약
## Policy Watch v1.8

> **적용 방법**
> `C:\policy-watch\SPEC-ADDENDUM-8.md` 로 저장 후 Claude Code에 입력:
> `SPEC-ADDENDUM-8.md 읽고 §1, §2 먼저 해줘`
>
> §3~§5(요약 기능)는 별도 작업이며 준비물이 필요하다. §6 참고.

---

# 0. 요청 사항과 난이도

| # | 요청 | 성격 | 난이도 |
|---|---|---|---|
| 1 | 검색어로 조회하는 기능 | 프론트엔드 | 낮음 |
| 2 | 시행일 캘린더 정렬 개선 | 프론트엔드 | 낮음 |
| 3 | 내용 요약 + 준비사항 3줄 | **수집 + AI 필요** | **높음** |

3번은 앞의 둘과 성격이 다르다. 두 가지 전제가 필요하다.

- **본문 수집**: 현재는 목록에서 제목·날짜·링크만 가져온다. 요약할 원문 자체가 없다.
- **판단 생성**: "팜한농이 준비할 것"은 규칙으로 만들 수 없다. 현재 `impact` 문구는 카테고리별 고정 템플릿이라 모든 항목에 동일하게 붙는다.

---

# 1. 검색 기능

## 1-1. 적용 화면
- 오늘의 정책동향
- 전체 동향 (공식 자료 / 관련 보도)

## 1-2. UI

필터 바 우측에 검색 입력창을 배치한다.

```
[전체][K-IFRS][세법][내부회계][ESG]  [문서 종류 ▾]     [🔍 제목·기관 검색_______]
시작일 [____] ~ 종료일 [____] [조회]  [최근 7일][30일][90일]
```

- placeholder: `제목, 기관명으로 검색`
- 입력 시 **즉시 필터링** (디바운스 200ms). 별도 검색 버튼 없음
- 입력값이 있으면 우측에 `✕` 초기화 버튼
- 결과 건수 표시에 반영: `총 12건 (검색: "법인세")`

## 1-3. 검색 대상 필드

```javascript
const searchTargets = [
  item.title,
  item.source.name,
  (item.summary || []).join(" "),
  item.impact || "",
  (item.matched_keywords || []).join(" "),
];
```

## 1-4. 매칭 규칙

```javascript
function matchesQuery(item, query) {
  if (!query) return true;
  const q = norm(query);
  // 공백으로 나눈 모든 토큰이 포함되어야 함 (AND)
  const tokens = q.split(/\s+/).filter(Boolean);
  const hay = norm(searchTargets.join(" "));
  return tokens.every(t => hay.includes(t));
}
```

- 공백 제거·소문자 변환 후 비교 (기존 `norm()` 재사용)
- 여러 단어 입력 시 AND 조건 (`법인세 개정` → 둘 다 포함)
- 대소문자·띄어쓰기 무시

## 1-5. URL 동기화

기존 필터와 동일하게 쿼리스트링에 반영해 링크 공유가 가능하게 한다.

```
?cat=tax&q=법인세&from=2026-06-01
```

## 1-6. 결과 없을 때

```
"○○"에 해당하는 항목이 없습니다.
[검색어 지우기]  [전체 기간으로 조회]
```

---

# 2. 시행일 캘린더 정렬

## 2-1. 문제

일정이 73건으로 늘면서 과거 시행일(2018~2025년)이 목록 앞에 나온다. 사용자가 먼저 봐야 할 것은 **앞으로 시행될 일정**이다.

## 2-2. 정렬 규칙

```
1순위: 미래 일정 (effective_date >= 오늘)  — 가까운 순 (오름차순)
2순위: 과거 일정 (effective_date < 오늘)   — 최근 순 (내림차순)
```

미래 일정이 먼저, 그 안에서는 임박한 것부터. 과거 일정은 그 아래에 최근 것부터 쌓인다.

```javascript
function sortSchedules(schedules) {
  const today = new Date(); today.setHours(0,0,0,0);
  const future = [], past = [];
  for (const s of schedules) {
    (new Date(s.effective_date) >= today ? future : past).push(s);
  }
  future.sort((a,b) => new Date(a.effective_date) - new Date(b.effective_date));
  past.sort((a,b) => new Date(b.effective_date) - new Date(a.effective_date));
  return [...future, ...past];
}
```

## 2-3. 화면 구분

두 그룹 사이에 구분선과 라벨을 넣는다.

```
시행 예정
  D-123  2027.01.01  제1118호 재무제표 표시와 공시
  D-201  2027.03.15  ...
─────────────────────────
시행 완료
  2026.01.01  자연에 의존하는 전력과 관련된 계약
  2025.12.31  제1117호 보험계약
```

- 과거 항목은 회색 처리, D-Day 대신 날짜만 표시
- 기본은 미래 전체 + 과거 10건. 하단에 `과거 일정 더 보기` 버튼

## 2-4. 사이드바 요약 박스

우측 사이드바의 `시행 예정 일정`(당월~향후 3개월)은 미래 일정만 다루므로 오름차순 유지. 변경 없음.

---

# 3. 본문 수집 (요약의 전제)

## 3-1. 현재 상태

각 어댑터는 목록 페이지에서 제목·날짜·링크만 추출한다. 실측 결과 **117건 중 92건이 빈 요약**이었던 이유가 이것이다.

## 3-2. 조치

**공식 소스(L1/L2)에 한해** 상세 페이지 본문을 가져온다.

```python
# sources/_fetch_body.py 신설

def fetch_body(url: str, selector: str | None = None) -> str | None:
    """상세 페이지 본문 텍스트 추출.

    - 실패해도 예외를 던지지 않고 None 반환
    - 최대 3000자로 절단
    - 요청 간 2초 sleep
    """
```

**어댑터별 본문 선택자**는 Phase 3A 조사 때 확인한 구조를 활용한다. 확인이 안 된 소스는 조사 후 추가한다.

## 3-3. 저작권 원칙 (중요)

| 대상 | 처리 |
|---|---|
| 공식 기관 자료 | 본문을 **요약 생성에만 사용하고 저장하지 않는다.** `data.json`에는 생성된 요약만 담는다 |
| 언론 기사 | **본문을 수집하지 않는다.** 제목과 링크만 다룬다 |
| 첨부 PDF/HWP | 다운로드·파싱하지 않는다 (기존 방침 유지) |

뉴스는 RSS가 제공하는 짧은 설명문(description)이 있으면 그것만 활용하되, 없으면 요약 없이 제목만 표시한다.

## 3-4. 부하 관리

- 본문 수집은 **신규 항목에만** 수행한다. 이미 수집한 `id`는 건너뛴다
- 하루 신규는 대개 10~30건 수준이므로 부담이 크지 않다
- 실패 시 해당 항목은 요약 없이 진행. 전체를 중단하지 않는다

---

# 4. 요약·준비사항 생성

## 4-1. 출력 형식

각 항목에 3줄을 생성한다.

```json
{
  "summary": [
    "K-IFRS 제1118호가 제정되어 재무제표 표시와 공시 요구사항이 전면 개편된다.",
    "손익계산서 구분 표시와 경영진성과측정치(MPM) 공시가 새로 요구된다."
  ],
  "impact": "2027년 재무제표부터 적용되므로 2026년 중 손익 구분 체계 재설계가 필요하다."
}
```

- `summary` 2줄: 무엇이 어떻게 바뀌는가
- `impact` 1줄: 팜한농(제조업 상장·연결 실체)이 준비할 것

## 4-2. 생성 방식

Anthropic API를 사용한다. 규칙 기반으로는 실질적인 판단을 만들 수 없다.

```python
# sources/_summarize.py 개편

SYSTEM_PROMPT = """당신은 제조업 상장기업의 재경 실무를 지원한다.
회사 정보: 팜한농 — 작물보호제·비료·종자 제조업, 외부감사 대상, 연결 실체.
K-IFRS 적용 대상이며 금융업·공공기관·비영리법인이 아니다.

주어진 규제 자료를 읽고 다음 형식의 JSON만 출력한다. 다른 말은 하지 않는다.

{
  "summary": ["<무엇이 바뀌는지 1줄>", "<핵심 내용 1줄>"],
  "impact": "<이 회사가 준비할 것 1줄. 없으면 null>"
}

규칙:
- 각 줄은 80자 이내, 평서문
- 원문에 없는 내용을 추측해 쓰지 않는다
- 시행일이 명시되어 있으면 impact에 포함한다
- 이 회사와 무관한 내용이면 impact를 null로 둔다
- 확정 여부를 분명히 한다 (공개초안이면 "확정 시" 등으로 표현)
"""
```

**모델 선택:** 비용 대비 성능을 고려해 Haiku 계열로 시작한다. 품질이 부족하면 Sonnet으로 올린다.

## 4-3. 캐싱 (필수)

한 번 요약한 항목을 다시 호출하지 않는다.

```
data/summary_cache.json
{
  "<item_id>": {
    "summary": [...],
    "impact": "...",
    "generated_at": "2026-08-31T10:00:00+09:00",
    "model": "..."
  }
}
```

- 매일 신규 항목만 API를 호출한다
- 캐시 파일은 git에 커밋해 GitHub Actions에서도 재사용한다
- 캐시 적중률을 로그로 출력한다: `[요약] 신규 12건 생성, 캐시 93건 재사용`

## 4-4. 비용 관리

```yaml
# data/filters.yml
summarize:
  enabled: true
  max_new_per_run: 30      # 1회 실행당 상한. 초과분은 다음 실행으로
  min_tier: 2              # tier 3 이하(일반 뉴스)는 요약하지 않음
  skip_doc_types:          # 요약이 불필요한 문서 종류
    - 상설자료
    - 논의자료
    - 해외기준
```

**공식 자료 위주로 제한한다.** 뉴스까지 요약하면 비용은 늘고 가치는 낮다. 뉴스는 제목과 링크로 충분하다.

## 4-5. API 키

```
환경변수: ANTHROPIC_API_KEY
GitHub Secrets에 동일 이름으로 등록
```

키가 없으면 **요약 없이 정상 동작**한다. 기존 규칙 기반 로직을 폴백으로 유지한다.

---

# 5. 안전장치

## 5-1. 화면 표시

자동 생성 요약임을 카드에서 인지할 수 있어야 한다.

```
┌────────────────────────────────────┐
│ [K-IFRS] [제·개정] [확정]          │
│ 제1118호 재무제표 표시와 공시      │
│ · 요약 1줄                          │
│ · 요약 2줄                          │
│ ┌────────────────────────────────┐ │
│ │ 준비사항 (AI 생성)              │ │  ← 라벨 명시
│ │ 2027년 재무제표부터 적용...     │ │
│ └────────────────────────────────┘ │
│ [원문 보기 🔗]                      │
└────────────────────────────────────┘
```

- `준비사항` 박스에 `(AI 생성)` 라벨을 작게 병기
- 푸터 고지 문구는 이미 반영됨 (ADDENDUM-4 §6)

## 5-2. 생성 실패 시

- API 오류 → 요약 없이 제목만 표시. 카드가 깨지지 않게 할 것
- JSON 파싱 실패 → 재시도 1회 후 포기
- 빈 문자열이나 의미 없는 출력 → 렌더하지 않음

## 5-3. 검증 요구사항

첫 실행 후 **생성된 요약 10건을 사용자가 직접 검토**한다. 다음을 확인할 것.

- 원문에 없는 내용을 지어내지 않았는가
- 공개초안을 확정된 것처럼 서술하지 않았는가
- 시행일이 정확한가
- "준비사항"이 실무적으로 타당한가

부정확한 사례가 나오면 프롬프트를 조정한다. **회계·세무 정보는 틀린 요약이 없느니만 못하다.**

---

# 6. 작업 순서

| 순서 | 작업 | 준비물 | 비고 |
|---|---|---|---|
| 1 | §1 검색 기능 | 없음 | 즉시 가능 |
| 2 | §2 캘린더 정렬 | 없음 | 즉시 가능 |
| 3 | §3 본문 수집 | 없음 | 소스별 선택자 조사 필요 |
| 4 | §4 요약 생성 | **Anthropic API 키** | 아래 참고 |
| 5 | §5 화면 반영 | 없음 | |

## 6-1. API 키 발급

1. https://console.anthropic.com 접속·가입
2. API Keys 메뉴에서 키 생성
3. 로컬 테스트: `C:\policy-watch\.env` 파일에 `ANTHROPIC_API_KEY=...` 저장
   (`.gitignore`에 `.env`가 이미 있어 공개되지 않음)
4. 배포용: GitHub Secrets에 `ANTHROPIC_API_KEY`로 등록

**키를 Claude Code에게 알려주지 말 것.** 코드에 하드코딩될 수 있다. 사용자가 직접 파일에 넣는다.

## 6-2. 비용

신규 항목에만 호출하고 캐싱하므로 하루 10~30건 수준이다. Haiku 계열 기준으로 소액이 예상되나, 실제 요금은 콘솔에서 확인할 것. `max_new_per_run` 값으로 상한을 걸어둔다.

## 6-3. 단계적 도입 권고

`enabled: false`로 두고 **로컬에서 10~20건만 생성해 품질을 확인한 뒤** 전면 적용할 것. 요약 품질이 기대에 못 미치면 프롬프트 조정이나 모델 변경이 필요하다.

---

# 7. Claude Code에 입력할 지시

## 7-1. 1차 (즉시 가능)

```
SPEC-ADDENDUM-8.md 읽고 §1(검색 기능)과 §2(캘린더 정렬) 해줘.
브라우저에서 확인할 수 있게 로컬 서버도 띄워줘.
```

## 7-2. 2차 (본문 수집)

```
§3 본문 수집 해줘. 공식 소스만, 뉴스는 제외.
소스별로 본문 선택자를 먼저 조사하고 몇 건 뽑아서 보여줘.
```

## 7-3. 3차 (요약 생성)

```
§4 요약 기능 해줘. API 키는 .env에 넣어뒀어.
먼저 enabled=false 상태로 만들고, 테스트로 10건만 생성해서 보여줘.
품질 확인하고 전면 적용할지 정할게.
```
