# SPEC 보완서 4 — 화면 검수 반영 및 "오늘의 정책동향" 신설
## Policy Watch v1.4

> **적용 방법**
> `C:\policy-watch\SPEC-ADDENDUM-4.md` 로 저장 후 Claude Code에 입력:
> `SPEC-ADDENDUM-4.md 읽고 순서대로 진행해줘`
>
> 이 문서는 Phase 5 실화면 검수 결과를 반영한다. 앞선 ADDENDUM 1~3은 그대로 유효하다.

---

# 0. 작업 순서

아래 순서대로 진행하고, 각 단계가 끝나면 화면을 새로고침해 확인할 수 있도록 보고할 것.

| 순서 | 작업 | 난이도 |
|---|---|---|
| 1 | §1 인사·조직 보도자료 제외 | 낮음 |
| 2 | §2 상시 비치 자료 처리 | 중간 |
| 3 | §3 유사 기사 중복 제거 강화 | 중간 |
| 4 | §4 공식-뉴스 연결 | 중간 |
| 5 | §5 "오늘의 정책동향" 탭 신설 | **높음** |
| 6 | §6 기관 바로가기 푸터 | 낮음 |

---

# 1. 인사·조직 보도자료 제외

## 문제
`금융위원회 인사보도(과장급 전보)`가 K-IFRS 카테고리에 수집됐다. 규제 내용과 무관하다.

## 원인
`금융위원회`가 `kifrs` 카테고리의 필수 키워드라, 금융위가 발행한 모든 보도자료가 후보로 들어온다. Phase 3D에서 제목의 기관명 토큰을 제거하도록 고쳤으나, 이 항목은 남은 텍스트("인사보도(과장급 전보)")가 짧아 통과한 것으로 보인다.

## 조치

`_config.py`에 조직 운영 관련 제외 키워드를 추가한다. **L1/L2 공식 소스에도 적용한다** — 노이즈 필터 면제 대상에서 이 목록만은 예외로 둔다.

```python
# 기관의 조직 운영 관련 공지. 규제 내용과 무관하므로 전 계층에서 제외.
ADMIN_NOISE_KEYWORDS = [
    # 인사·조직
    "인사보도", "인사발령", "전보", "승진", "임명", "취임", "이임", "퇴임",
    "조직개편", "직원 채용", "채용 공고", "인턴 모집",
    # 기관 운영
    "청사 이전", "시스템 점검", "서비스 점검", "홈페이지 점검", "임시 휴무",
    "정기 휴관", "본인 인증", "이용 안내", "접속 장애",
    # 행사·의례
    "시무식", "종무식", "기념식", "체육대회", "봉사활동", "간담회 개최 안내",
]


def is_admin_noise(title: str) -> bool:
    """기관 운영성 공지 판정. 계층과 무관하게 적용."""
    t = _norm(title)
    return any(_norm(k) in t for k in ADMIN_NOISE_KEYWORDS)
```

**적용 위치:** 각 어댑터의 `fetch()` 파싱 단계. `classify()` 호출 이전에 걸러낸다.

**주의:** `간담회`, `회의` 같은 단어를 통째로 막으면 안 된다. "회계기준위원회 회의 결과" 같은 실질 내용이 사라진다. 위 목록은 조직 운영에 한정된 표현만 담았다.

## 테스트

| 제목 | 기대 |
|---|---|
| 금융위원회 인사보도(과장급 전보) | 제외 |
| 본인 인증 서비스 일시 점검 안내 | 제외 |
| 회계기준위원회 제12차 회의 결과 | **통과** |
| ESG 공시 관련 업계 간담회 결과 발표 | **통과** |

---

# 2. 상시 비치 자료(Static Reference) 처리

## 문제
`내부회계관리제도 평가 및 보고 모범규준 전문(2021.10.1. 개정)` 등 2021년 문서가 오늘 날짜로 표시된다. 총 38건 중 상당수가 이 유형이다.

## 원인
운영위원회 자료실은 **현재 유효한 문서를 상시 비치하는 게시판**이다. 신규 게시 소식이 아니라 상설 참고자료다. `published_at`을 못 찾아 수집일로 대체하면서 "오늘 나온 자료"로 보이게 됐다.

KASB A2(일반기업회계기준 76건)와 같은 성격의 문제다.

## 조치

### 2-1. 제목에서 개정일 추출

문서 제목에 개정일이 명시된 경우가 많다. 이를 `published_at`으로 사용한다.

```python
# 매칭 패턴 예시
"(2021.10.1. 개정)"     → 2021-10-01
"('24.12.23 개정)"      → 2024-12-23
"(2023. 6. 30. 제정)"   → 2023-06-30
"Clean ver.('24.12.23)" → 2024-12-23
```

추출 실패 시에만 수집일로 대체하되, 그 경우 `date_estimated: true` 플래그를 세운다.

### 2-2. `is_static` 플래그 신설

```json
{
  "is_static": true,
  "published_at": "2021-10-01",
  "date_estimated": false
}
```

**판정 기준**
- 게시판 자체가 상시 비치형인 경우 어댑터에서 `is_static=True`로 고정 (운영위 자료실, 금감원 기준 페이지 등)
- Phase 3A 조사 결과에 따라 소스별로 지정한다

### 2-3. 화면 처리

| 위치 | 처리 |
|---|---|
| **오늘의 정책동향** (§5) | **제외.** 새 소식이 아님 |
| 전체 동향 피드 | 포함하되 날짜 그룹은 `published_at` 기준. 카드에 `상설자료` 회색 뱃지 표시 |
| 기본 정렬 | 상시 자료는 신규 항목보다 아래로 |

### 2-4. 별도 진입 경로 제공

상시 비치 자료는 **없애면 안 된다.** 내부회계팀이 모범규준·적용기법 전문을 찾을 때 유용하다. 다만 "오늘의 소식"과 섞이지 않아야 한다.

문서종류 필터에 `상설자료만` 빠른 버튼을 추가한다.

---

# 3. 유사 기사 중복 제거 강화

## 문제
```
남부발전, 내부통제 고도화 '앞장'…6대 중점관리 분야 분과위 가동 — 에너지플랫폼뉴스
남부발전, 내부통제 고도화 위한 '6대 중점관리 분야' 분과위 가동 — 스트레이트뉴스
```
같은 보도자료를 받아쓴 기사가 각각 남는다. Phase 1에서 "제목 전체 일치"로 완화한 결과다.

## 원인 및 방향
당시엔 실데이터가 없어 보수적으로 잡았으나, 이제 실측 근거가 생겼다. **완전 일치는 너무 느슨하고, 앞 40자 비교는 너무 위험하다.** 중간 지점을 택한다.

## 조치: 토큰 자카드 유사도

```python
def title_similarity(a: str, b: str) -> float:
    """제목을 어절 단위로 쪼개 자카드 유사도 계산."""
    ta = set(re.findall(r"[가-힣A-Za-z0-9]+", a))
    tb = set(re.findall(r"[가-힣A-Za-z0-9]+", b))
    if not ta or not tb:
        return 0.0
    return len(ta & tb) / len(ta | tb)


SIMILARITY_THRESHOLD = 0.65   # 실데이터 보고 조정 가능
```

**적용 규칙**
- **L3(뉴스)에만 적용.** L1/L2 공식 자료는 완전 일치만 중복 처리
- 같은 카테고리 내에서만 비교
- 유사도가 임계값 이상이면 `final_score`가 높은 쪽(= 신뢰도 높은 매체)만 남긴다
- 남긴 항목에 `duplicate_count`를 기록해 "외 3건 보도" 표시에 사용

```json
{
  "duplicate_count": 3,
  "duplicate_sources": ["에너지플랫폼뉴스", "스트레이트뉴스", "핀포인트뉴스"]
}
```

## 주의
비교 대상이 많으면 계산량이 커진다. 카테고리별로 나눈 뒤 `published_at`이 3일 이내인 것끼리만 비교해 범위를 줄일 것.

## 테스트

| 제목 A | 제목 B | 기대 |
|---|---|---|
| 남부발전, 내부통제 고도화 '앞장'…6대 중점관리 분야 분과위 가동 | 남부발전, 내부통제 고도화 위한 '6대 중점관리 분야' 분과위 가동 | **중복** |
| 법인세법 시행령 개정안 입법예고 — 접대비 한도 | 법인세법 시행령 개정안 입법예고 — 감가상각 특례 | **중복 아님** |
| ESG 공시 로드맵 연기 | ESG 공시 의무화 로드맵 핵심쟁점 6가지 | **중복 아님** |

세 번째 케이스가 임계값 조정의 기준이 된다. 실행 후 결과를 보고 0.6~0.75 범위에서 조정할 것.

---

# 4. 공식 항목에 관련 뉴스 연결

## 목적
공식 원문은 딱딱하고 첨부 PDF 위주다. 실무자는 전문지 해설을 먼저 읽고 원문으로 넘어가는 경우가 많다. 두 계층을 연결하면 대시보드의 실질 가치가 크게 오른다.

## 조치

L1/L2 항목에 관련 L3 기사를 최대 3건까지 붙인다.

```json
{
  "id": "abc123",
  "category": "esg",
  "title": "지속가능성 공시기준 제2호 공개초안 발표",
  "related_news": [
    {
      "title": "KSSB, ESG공시 기준서 권고안 의결 연기",
      "source": "임팩트온",
      "url": "https://...",
      "published_at": "2026-08-20"
    }
  ]
}
```

## 매칭 규칙

```
조건 1: 같은 카테고리
조건 2: published_at 차이 7일 이내
조건 3: title_similarity >= 0.30  (§3의 함수 재사용, 낮은 임계값)
        또는 공식 항목 제목의 핵심 명사구가 기사 제목에 포함

정렬: final_score 내림차순, 최대 3건
```

**핵심 명사구 추출:** 공식 제목에서 조사·기관명을 제거하고 남는 2어절 이상 연속 표현. 예를 들어 "지속가능성 공시기준 제2호" 같은 부분.

## 화면 처리

공식 카드 하단, 버튼 행 위에 접이식으로 배치한다.

```
┌────────────────────────────────────┐
│ [ESG] [공개초안] [의견수렴]        │
│ 지속가능성 공시기준 제2호 공개초안 │
│ ...                                │
│ ▸ 관련 보도 3건                    │  ← 클릭 시 펼침
│ [뉴스 기사 보기] [공식 원문 🔗]    │
└────────────────────────────────────┘
```

- 기본 접힘 상태
- 펼치면 매체명 + 제목 + 날짜, 각각 새 창 링크
- `related_news`가 비면 이 행 자체를 렌더하지 않는다

## 부수 효과
관련 뉴스로 붙은 L3 항목은 피드에서 **중복 노출하지 않는다.** 공식 카드 안에서만 보이게 해 피드가 정리된다.

---

# 5. "오늘의 정책동향" 탭 신설 ★ 핵심 작업

## 목적
첫 화면에서 **오늘 새로 나온 것만** 카드 형태로 보여준다. 실무자가 아침에 3분간 훑는 용도다.

## 5-1. 네비게이션 구조 변경

```
[PW] Policy Watch   [오늘의 정책동향] [전체 동향] [시행일 캘린더] [내가 찜한 문서]
```

- **기본 진입 화면 = 오늘의 정책동향**
- `전체 동향`: 기존 70:30 화면 (필터 + 피드 + 사이드바)
- `시행일 캘린더`: 캘린더 전체 화면 + 일정 리스트
- `내가 찜한 문서`: 북마크 목록 (localStorage 사용 불가 → 후순위, §5-6 참고)

## 5-2. "오늘"의 정의

```
기준: published_at 이 최근 수집 실행일과 같은 날
예외: 오늘 항목이 5건 미만이면 최근 3일로 확대하고 안내 문구 표시
      ("최근 3일간의 소식입니다")
제외: is_static == true 인 항목 전부
```

주말·공휴일에는 공식 발표가 없어 0건이 나올 수 있다. 이 경우를 반드시 처리한다.

## 5-3. 화면 구성

```
┌───────────────────────────────────────────────────────────┐
│  2026년 8월 28일 (금) 소식        공식 6건 · 보도 12건     │
├───────────────────────────────────────────────────────────┤
│  공식 기관 발표                                            │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐          │
│  │▔▔▔▔▔ 세법  │ │▔▔▔▔▔ K-IFRS│ │▔▔▔▔▔ 내부회계│         │
│  │ [공식기관]   │ │ [공식기관]   │ │ [공식기관]   │         │
│  │ 제목        │ │ 제목        │ │ 제목        │          │
│  │ 요약 2줄    │ │ 요약 2줄    │ │ 요약 2줄    │          │
│  │ ┌─────────┐ │ │ ┌─────────┐ │ │ ┌─────────┐ │          │
│  │ │회사 영향│ │ │ │회사 영향│ │ │ │회사 영향│ │          │
│  │ └─────────┘ │ │ └─────────┘ │ │ └─────────┘ │          │
│  │ ▸관련보도 2 │ │             │ │             │          │
│  │ 기관·날짜 [원문→]           │ │             │          │
│  └─────────────┘ └─────────────┘ └─────────────┘          │
│                                                            │
│  언론 보도                                                 │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐          │
│  │  (동일 구조, 좌측 컬러바만 연하게)          │          │
│  └─────────────┘ └─────────────┘ └─────────────┘          │
└───────────────────────────────────────────────────────────┘
```

## 5-4. 카드 디자인

- **그리드:** `≥1280px` 3열 / `768~1279px` 2열 / `<768px` 1열
- **좌측 컬러바:** 카드 왼쪽에 4px 세로 바, 카테고리 색상. 공식은 진하게, 보도는 40% 투명도
- **뱃지:** 카테고리 + `공식기관` 또는 매체명
- **제목:** 16px, 600, 최대 3줄 말줄임
- **요약:** 14px, 최대 2줄. 없으면 영역 자체를 렌더하지 않음
- **회사 영향 박스:** 연한 배경 + 좌측 3px 바. `impact`가 null이면 미표시
- **하단:** 좌측에 `기관명 · 날짜`, 우측에 `원문 →` 버튼
- 카드 높이는 그리드 내에서 균일하게 (flex, 하단 정렬)

## 5-5. 정렬

```
공식 그룹: tier 오름차순 → final_score 내림차순
보도 그룹: final_score 내림차순
각 그룹 최대 12건. 초과 시 "더 보기" 버튼 → 전체 동향 탭으로 이동
```

## 5-6. 북마크 관련 주의

**브라우저 localStorage는 사용하지 않는다.** 기존 구현에 들어가 있다면 제거할 것.

대안: `내가 찜한 문서` 탭은 이번 범위에서 제외하고, 네비게이션에서도 뺀다. 향후 별도 검토.

---

# 6. 기관 바로가기 푸터

전 화면 하단에 고정 배치한다.

```html
<footer>
  <div class="footer-links">
    <span class="footer-label">주요 기관 바로가기</span>
    <!-- 각 링크는 target="_blank" rel="noopener" -->
  </div>
  <p class="footer-note">...</p>
</footer>
```

**링크 목록**

| 표시명 | URL |
|---|---|
| 한국회계기준원 | https://www.kasb.or.kr |
| 금융위원회 | https://www.fsc.go.kr |
| 금융감독원 | https://www.fss.or.kr |
| 국세청 | https://www.nts.go.kr |
| 국가법령정보센터 | https://www.law.go.kr |
| 기획재정부 | https://www.moef.go.kr |
| 한국공인회계사회 | https://www.kicpa.or.kr |
| 내부회계관리제도운영위원회 | (Phase 3A에서 확인한 k-icfr.org 주소) |

**디자인**
- 배경: `--navy-deep` (`#172554`), 텍스트 흰색
- 링크는 텍스트 버튼 형태, hover 시 밑줄
- 모바일에서는 2열 그리드로 줄바꿈

**하단 고지 문구** (작은 회색 텍스트, 12px)

```
Policy Watch는 공개된 공식 원문과 언론 보도의 서지 정보를 수집·정리합니다.
요약과 실무 의견은 참고용 자동 생성 정보이며, 실제 적용은 원문과 전문가 확인이 필요합니다.
원문 저작권은 각 기관·매체에 있습니다.
```

이 문구는 **반드시 포함한다.** 자동 생성 요약을 실무 판단 근거로 삼는 것을 방지하는 목적이다.

---

# 7. 검증 요구사항

각 작업 후 다음을 확인하고 보고할 것.

| 항목 | 확인 방법 |
|---|---|
| §1 | `금융위원회 인사보도`가 결과에서 사라졌는지 |
| §2 | 2021년 모범규준의 `published_at`이 2021-10-01인지, `is_static`이 true인지 |
| §3 | 남부발전 기사가 1건으로 줄었는지, `duplicate_count`가 기록됐는지 |
| §4 | ESG 공식 항목에 KSSB 관련 기사가 붙었는지 |
| §5 | 첫 화면이 오늘의 정책동향인지, 공식/보도 그룹이 나뉘는지 |
| §6 | 푸터 링크가 전부 새 창으로 열리는지 |

**전체 테스트를 다시 통과시킬 것.** 신규 로직에 대한 테스트도 추가한다.

브라우저 렌더링 확인은 사용자가 직접 한다. 스크린샷 촬영을 위한 브라우저 확장 설치는 요청하지 않는다.

---

# 8. Claude Code에 입력할 지시

```
SPEC-ADDENDUM-4.md 읽어줘. 화면 검수 결과를 반영한다.

§0의 순서대로 진행하되, §1~§4를 먼저 끝내고 보고해줘.
§5(오늘의 정책동향)는 그다음에 별도로 하자.

localStorage는 쓰지 마. 기존 코드에 있으면 제거해줘.
```
