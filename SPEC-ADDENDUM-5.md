# SPEC 보완서 5 — 사업 연관성 필터 및 중복 제거 개선
## Policy Watch v1.5

> **적용 방법**
> `C:\policy-watch\SPEC-ADDENDUM-5.md` 로 저장 후 Claude Code에 입력:
> `SPEC-ADDENDUM-5.md 읽고 §1부터 순서대로 진행해줘`

---

# 0. 진단

Phase 5 실화면 검수에서 확인된 부적합 항목과 원인.

| 수집된 항목 | 문제 |
|---|---|
| 타에브의 바시즈 사령관 복귀, 모즈타바의 내부 통제 강화 포석 | 군사·정치 기사. `내부통제`가 문자 그대로 매칭 |
| 남부발전, 내부통제 고도화 '앞장'…6대 중점관리 분야 분과위 가동 | 타사 자사 홍보성 보도. 규제 변경 아님 |
| 캠코-한수원, AI 활용 감사기법 고도화 맞손 | 동일 |
| 신협 NPL 정리체계 제도화…대부채권 양수자격 부여 | 금융업 고유 사안 |
| 사회복지법인 전문인력 확보 절실…법인세법 시행령 개정 선행돼야 | 복지법인 특수 이슈 |
| 남부발전 관련 기사 4건 잔존 | 매체명 접미사로 유사도 희석, 중복 제거 실패 |

## 근본 원인 세 가지

1. **규제성 판정이 없다.** 현재는 키워드가 걸리면 통과시킨다. "규제·제도가 바뀌었다"와 "어떤 회사가 뭘 했다"를 구분하지 않는다.
2. **`내부통제` 키워드가 지나치게 넓다.** 우리가 원하는 것은 `내부회계관리제도`인데, `내부통제`는 군사·보안·경영 전 분야의 일반 명사다.
3. **중복 비교 시 매체명이 포함된다.** `- 스트레이트뉴스` 같은 접미사가 어절 집합에 들어가 유사도를 떨어뜨린다.

## 사용자 조직 정보 (필터 설계 기준)

- 회사: 팜한농 (제조업 — 작물보호제·비료·종자)
- 부서: CFO부문 회계팀
- 관심사: 제조업 상장·연결 실체에 적용되는 회계기준·세법·ESG 공시·내부회계관리제도 **변경사항**
- 무관: 금융업·공공기관·복지법인 고유 규제, 타사 경영 활동, 군사·정치

---

# 1. 규제성 판정 게이트 (가장 근본적 조치)

L3(뉴스)는 **"제도가 바뀌었다"는 내용일 때만** 통과시킨다.

## 1-1. 구현

```python
# _config.py

# 규제·제도 변경을 나타내는 표현. L3 통과의 필요조건.
REGULATORY_SIGNALS = [
    # 입법·제정 절차
    "개정", "제정", "공포", "시행", "입법예고", "의결", "공표", "확정",
    "개정안", "제정안", "공개초안", "의견수렴", "의견조회", "심의",
    # 기준·지침
    "기준서", "모범규준", "가이드라인", "적용지침", "해석", "지침",
    "질의회신", "예규", "유권해석", "집행기준",
    # 제도 도입·적용
    "의무화", "도입", "적용 대상", "적용 시기", "유예", "면제", "완화", "강화",
    "로드맵", "단계적 적용",
    # 발표 주체 행위
    "발표", "배포", "발간", "안내", "고시",
]


def has_regulatory_signal(title: str) -> bool:
    """제목에 제도 변경 신호가 있는지."""
    t = _norm(title)
    return any(_norm(k) in t for k in REGULATORY_SIGNALS)
```

## 1-2. 적용 규칙

```
L1/L2 (공식 소스)  → 게이트 미적용. 기관 발표는 그 자체가 규제 정보
L3 (뉴스)          → has_regulatory_signal(title) == False 이면 제외
```

## 1-3. 효과 예상

| 제목 | 판정 |
|---|---|
| 타에브의 바시즈 사령관 복귀…내부 통제 강화 포석 | **제외** (강화는 있으나 §3에서 재차단) |
| 남부발전, 내부통제 고도화 위한 분과위 가동 | **제외** (신호 없음) |
| 캠코-한수원, AI 활용 감사기법 고도화 맞손 | **제외** |
| KSSB, ESG공시 기준서 권고안 의결 연기 | **통과** (기준서, 의결) |
| 2026년 정부 세제개편(안) - 국제조세 관련 주요 개정사항 | **통과** |

`강화`가 REGULATORY_SIGNALS에 있어 일부 오탐이 남는다. §3의 홍보성 판정과 §2의 업종 필터로 2차 차단한다.

---

# 2. 무관 업종·분야 제외

제조업 상장사와 무관한 영역을 명시적으로 배제한다.

```python
# _config.py

# 우리 업종과 무관한 분야. L3에만 적용.
OUT_OF_SCOPE_KEYWORDS = {
    # 군사·정치·국제정세
    "military_politics": [
        "사령관", "군부", "정권", "쿠데타", "대선", "총선", "개각",
        "국방", "전쟁", "무장", "테러", "외교부", "정상회담",
    ],
    # 금융업 고유 규제 (우리는 금융회사가 아님)
    "financial_industry": [
        "NPL", "부실채권", "대부채권", "여신", "수신", "저축은행",
        "신협", "새마을금고", "카드사", "캐피탈", "보험료율", "지급여력",
        "BIS비율", "예금보험", "전자금융업자", "가상자산사업자",
        "금융복합기업집단", "은행법", "보험업법", "자본시장법 개정",
    ],
    # 공공기관·지자체 고유
    "public_sector": [
        "공기업", "준정부기관", "지방공기업", "출자기관", "산하기관",
        "경영평가", "혁신계획", "지방재정", "예산 편성 지침",
    ],
    # 비영리·특수법인
    "nonprofit": [
        "사회복지법인", "의료법인", "학교법인", "종교단체", "비영리법인",
        "공익법인", "협동조합 기본법",
    ],
}


def is_out_of_scope(title: str) -> str | None:
    """무관 분야면 해당 분류명, 아니면 None."""
    t = _norm(title)
    for scope, kws in OUT_OF_SCOPE_KEYWORDS.items():
        if any(_norm(k) in t for k in kws):
            return scope
    return None
```

## 2-1. 예외 처리

**공익법인 관련 세법 개정**처럼 우리에게도 영향이 있는 경우가 있다. 다음 조건이면 무관 판정을 무시한다.

```
무관 키워드가 있어도, 제목에 "법인세법" / "부가가치세법" / "조세특례제한법" /
"K-IFRS" / "내부회계관리제도" 중 하나가 함께 있으면 통과시킨다.
```

단, `사회복지법인 전문인력 확보…법인세법 시행령 개정 선행돼야` 케이스는 여전히 통과한다. 이는 §4의 관련성 점수로 순위를 낮추는 방식으로 처리한다(제외가 아니라 후순위).

---

# 3. 타사 홍보성 보도 제외

특정 기업의 자체 활동 소식은 규제 정보가 아니다.

```python
# 기업 홍보·활동 동사. 규제 신호와 함께 없으면 홍보성으로 판정.
CORPORATE_PR_KEYWORDS = [
    "가동", "출범", "발족", "킥오프", "맞손", "협약", "MOU", "업무협약",
    "앞장", "박차", "총력", "선포", "다짐", "결의",
    "수상", "선정", "인증 획득", "달성", "돌파", "성과",
    "구축", "고도화", "확대", "강화 나서", "추진", "도입키로",
    "간담회 개최", "세미나 개최", "포럼 개최", "설명회 개최",
]


def is_corporate_pr(title: str) -> bool:
    """타사 홍보성 보도 판정.

    홍보 동사가 있으면서 강한 규제 신호(제도 변경 절차)가 없으면 홍보성.
    """
    t = _norm(title)
    has_pr = any(_norm(k) in t for k in CORPORATE_PR_KEYWORDS)
    if not has_pr:
        return False
    # 제도 변경 절차를 나타내는 강한 신호가 있으면 홍보성이 아님
    strong = ["개정", "제정", "공포", "입법예고", "공개초안", "기준서",
              "모범규준", "가이드라인", "질의회신", "예규", "의무화"]
    has_strong = any(_norm(k) in t for k in strong)
    return not has_strong
```

## 3-1. 판정 예시

| 제목 | 판정 | 근거 |
|---|---|---|
| 남부발전, 내부통제 고도화 위한 분과위 가동 | **제외** | 고도화·가동, 강한 신호 없음 |
| 캠코-한수원, AI 활용 감사기법 고도화 맞손 | **제외** | 고도화·맞손 |
| 남부발전, AI·제3자 리스크 통제범위 확대 | **제외** | 확대, 강한 신호 없음 |
| 금감원, 내부회계관리제도 평가 지침 개정 | **통과** | 개정 = 강한 신호 |
| 회계기준원, ESG 공시기준 설명회 개최 | **제외** | 설명회 개최 (단, L1이면 게이트 미적용으로 통과) |

---

# 4. `내부통제` 키워드 정밀화

## 4-1. 문제
`내부통제`는 군사·보안·경영 전반의 일반 명사다. 이 단어 하나로 매칭하면 이란 군부 기사까지 들어온다.

## 4-2. 조치

`icfr` 카테고리의 필수 키워드를 재정의한다.

```python
"icfr": {
    "label": "내부회계", "team": "내부회계관리팀", "color": "#b45309",
    # 단독으로 카테고리를 확정할 수 있는 키워드
    "required_strong": [
        "내부회계관리제도", "내부회계관리", "연결내부회계",
        "외감법", "외부감사법", "주식회사 등의 외부감사에 관한 법률",
    ],
    # 아래 문맥 키워드와 함께 있을 때만 유효한 약한 키워드
    "required_weak": ["내부통제", "내부 통제"],
    "weak_context": [
        "회계", "재무제표", "공시", "감사인", "외부감사", "재무보고",
        "감사위원회", "회계법인", "평가·보고", "모범규준", "K-IFRS",
    ],
    "combine": [
        "감사 전환", "모범규준", "취약점", "외감법 개정", "연결 내부회계",
        "평가 가이드라인", "내부통제 미비점", "설계·운영", "감사인 검토",
    ],
    "naver_queries": ["내부회계관리제도", "연결 내부회계", "외감법 개정",
                      "내부회계 모범규준"],
}
```

```python
def match_icfr(title: str) -> bool:
    t = _norm(title)
    c = CATEGORIES["icfr"]
    if any(_norm(k) in t for k in c["required_strong"]):
        return True
    if any(_norm(k) in t for k in c["required_weak"]):
        # 약한 키워드는 회계 문맥이 함께 있어야 인정
        return any(_norm(k) in t for k in c["weak_context"])
    return False
```

## 4-3. 효과

| 제목 | 판정 |
|---|---|
| 타에브…모즈타바의 내부 통제 강화 포석 | **제외** (회계 문맥 없음) |
| 남부발전, 내부통제 고도화 | **제외** (회계 문맥 없음) |
| 금융지주 내부통제 강화…감사위원회 역할 확대 | **통과** (감사위원회) |
| 내부회계관리제도 평가·보고 지침 개정 | **통과** (강한 키워드) |

**이 조치만으로도 §0 진단표의 절반이 해결된다.** 다른 카테고리에도 같은 방식을 검토할 것 — 특히 `kifrs`의 `금융위원회`가 동일한 성격의 문제를 일으키고 있다(`신협 NPL` 케이스).

## 4-4. `kifrs` 카테고리 동일 조치

```python
"kifrs": {
    "required_strong": [
        "K-IFRS", "한국채택국제회계기준", "회계기준원", "회계감리",
        "기업회계기준", "회계처리기준",
    ],
    "required_weak": ["금융위원회", "금융감독원"],
    "weak_context": [
        "회계", "감리", "재무제표", "공시", "기준서", "외부감사",
        "회계법인", "주석", "감사보고서",
    ],
    ...
}
```

---

# 5. 중복 제거 개선

## 5-1. 문제
남부발전 기사 4건이 잔존했다. `- 스트레이트뉴스`, `[로컬 게시판/남부발전]` 같은 부가 요소가 어절 집합에 포함돼 유사도가 희석됐다.

## 5-2. 조치 A — 제목 정제 후 비교

```python
MEDIA_SUFFIX_RE = re.compile(r"\s*[-–—|]\s*[^-–—|]{2,15}$")   # "- 스트레이트뉴스"
BRACKET_PREFIX_RE = re.compile(r"^\s*[\[\(【][^\]\)】]{1,30}[\]\)】]\s*")  # "[로컬 게시판/...]"
QUOTE_RE = re.compile(r"[\"'''\"\"''`]")


def clean_title_for_compare(title: str) -> str:
    """중복 비교 전용 제목 정제. 표시용 title은 건드리지 않는다."""
    t = title
    t = BRACKET_PREFIX_RE.sub("", t)
    t = MEDIA_SUFFIX_RE.sub("", t)
    t = QUOTE_RE.sub("", t)
    t = re.sub(r"[…·、,]", " ", t)
    return t.strip()
```

`title_similarity()`는 이 정제된 제목으로 비교한다.

## 5-3. 조치 B — 주체 기반 군집화

같은 기업·기관이 주체인 기사는 하나로 묶는다.

```python
def extract_subject(title: str) -> str | None:
    """제목 앞부분의 주체명 추출. '남부발전, ...' → '남부발전'"""
    t = clean_title_for_compare(title)
    m = re.match(r"^([가-힣A-Za-z0-9]{2,12})\s*[,·]", t)
    return m.group(1) if m else None


# 중복 판정 규칙 (L3 전용)
# 다음 중 하나라도 만족하면 동일 사안으로 본다:
#   (a) title_similarity(정제본) >= 0.55
#   (b) extract_subject()가 같고 + 같은 카테고리 + published_at 3일 이내
#       + 정제본 유사도 >= 0.35
```

임계값을 0.65 → 0.55로 낮추되, (b) 조건으로 오탐을 방지한다.

## 5-4. 주의 — 과다 병합 방지

다음은 **묶이면 안 된다.**

| 제목 A | 제목 B |
|---|---|
| 법인세법 시행령 개정안 — 접대비 한도 | 법인세법 시행령 개정안 — 감가상각 특례 |
| ESG 공시 로드맵 연기 | ESG 공시 의무화 로드맵 핵심쟁점 6가지 |

두 번째 쌍은 주체가 없으므로 (b)가 적용되지 않고, (a)에서 0.55를 넘는지 실측 확인이 필요하다. 넘는다면 0.60으로 상향한다.

---

# 6. 사업 관련성 점수 (제외가 아닌 순위 조정)

명확히 무관하진 않지만 우선순위가 낮은 항목을 아래로 내린다.

```python
# 제조업 상장·연결 실체 관련 가점 키워드
RELEVANCE_BOOST = [
    # 제조업 회계 이슈
    "재고자산", "유형자산", "감가상각", "무형자산", "연구개발비", "개발비",
    "충당부채", "복구충당", "리스", "수익인식", "원가",
    # 세무 이슈
    "통합투자세액공제", "연구인력개발비", "R&D 세액공제", "이전가격",
    "글로벌최저한세", "국제조세", "관세", "수출",
    # 상장·연결
    "상장회사", "상장사", "연결재무제표", "지배기업", "종속기업",
    "사업보고서", "분기보고서", "감사보고서",
    # ESG (화학·제조 관련)
    "온실가스", "탄소", "Scope 1", "Scope 2", "Scope 3", "공급망",
    "화학물질", "환경", "안전보건",
]

# 관련성 낮음 (제외는 아니나 감점)
RELEVANCE_PENALTY = [
    "중소기업 특례", "소상공인", "개인사업자", "1인 법인",
    "비상장 중소기업", "간이과세",
]
```

```python
def relevance_score(title: str, body: str = "") -> int:
    """0~100. final_score 계산에 반영."""
    t = _norm(title + " " + body)
    boost = sum(1 for k in RELEVANCE_BOOST if _norm(k) in t)
    penalty = sum(1 for k in RELEVANCE_PENALTY if _norm(k) in t)
    return max(0, min(100, 50 + boost * 15 - penalty * 20))
```

## 6-1. 최종 점수 재정의

```python
final_score = (trust * 0.40
             + keyword_score * 0.20
             + relevance_score * 0.25    # 신규
             + recency_score * 0.15)
```

`relevance_score`를 `items[]`에 기록해 나중에 조정 근거로 삼는다.

---

# 7. 처리 순서 (L3 뉴스)

```
1) classify()                     카테고리 분류 (§4의 강/약 키워드 규칙 적용)
2) has_regulatory_signal()        규제성 없으면 제외          ← §1
3) is_out_of_scope()              무관 업종이면 제외          ← §2
4) is_corporate_pr()              타사 홍보성이면 제외        ← §3
5) is_admin_noise()               기관 운영 공지면 제외       (ADDENDUM-4)
6) is_noise()                     투자·주가 노이즈면 제외
7) match_tax_subject()            세법이면 세목 화이트리스트  (ADDENDUM-3)
8) is_incident_noise()            사건사고면 제외
9) relevance_score()              관련성 점수 산출            ← §6
10) dedupe_similar_news()         중복 병합                   ← §5
11) 상한 적용 (15건/카테고리)
```

각 단계에서 제외된 건수를 로그로 출력할 것. 필터가 과하게 작동하는지 확인하기 위함이다.

```
[L3 필터] tax: 원본 71 → 규제성 42 → 업종 38 → 홍보성 35 → ... → 최종 12
```

---

# 8. 설정 파일화

위 키워드 목록은 운영하며 계속 조정하게 된다. **`data/filters.yml`로 분리**해 코드 수정 없이 편집할 수 있게 한다.

```yaml
# data/filters.yml
# 이 파일만 고치면 수집 정책이 바뀝니다. 코드는 건드릴 필요 없습니다.

regulatory_signals: [개정, 제정, 공포, ...]

out_of_scope:
  military_politics: [사령관, 군부, ...]
  financial_industry: [NPL, 여신, ...]
  public_sector: [공기업, 경영평가, ...]
  nonprofit: [사회복지법인, 의료법인, ...]

corporate_pr: [가동, 출범, 맞손, ...]

relevance_boost: [재고자산, 통합투자세액공제, ...]
relevance_penalty: [소상공인, 간이과세, ...]

dedup:
  similarity_threshold: 0.55
  subject_similarity_threshold: 0.35
  window_days: 3
```

파일이 없거나 파싱 실패 시 코드 내 기본값으로 동작하고 경고를 남긴다.

---

# 9. 테스트 필수 케이스

`tests/test_relevance.py` 신설.

| 제목 | 기대 | 검증 대상 |
|---|---|---|
| 타에브의 바시즈 사령관 복귀…내부 통제 강화 포석 | 제외 | §4 약키워드 문맥 |
| 남부발전, 내부통제 고도화 위한 분과위 가동 | 제외 | §3 홍보성 |
| 캠코-한수원, AI 활용 감사기법 고도화 맞손 | 제외 | §3 홍보성 |
| 신협 NPL 정리체계 제도화…대부채권 양수자격 | 제외 | §2 금융업 |
| 사회복지법인…법인세법 시행령 개정 선행돼야 | 통과·저점수 | §2 예외 + §6 |
| KSSB, ESG공시 기준서 권고안 의결 연기 | 통과 | §1 |
| 2026년 정부 세제개편(안) — 국제조세 관련 주요 개정사항 | 통과·고점수 | §6 가점 |
| 내부회계관리제도 평가·보고 지침 개정 | 통과 | §4 강키워드 |
| 남부발전 기사 4건 | **1건으로 병합** | §5 |
| 법인세법 시행령 개정안 접대비 / 감가상각 | **병합 안 됨** | §5 과다병합 방지 |

---

# 10. 실행 후 확인

재수집 후 다음을 보고할 것.

1. 카테고리별 필터 단계별 제외 건수 (§7 로그 형식)
2. 최종 건수 — **너무 많이 걸러졌는지 확인이 필요하다.** 카테고리당 3건 미만이면 필터가 과한 것
3. 각 카테고리 상위 5건 제목 — 사용자가 실무 적합성을 직접 판단

**과다 필터링이 과소 필터링보다 위험하다.** 놓친 규제는 화면에 나타나지 않으므로 발견할 수 없다. 애매하면 통과시키고 점수를 낮추는 쪽을 택할 것.

---

# 11. Claude Code에 입력할 지시

```
SPEC-ADDENDUM-5.md 읽어줘. 사업 연관성 필터를 추가한다.

우리 회사는 제조업(팜한농 - 작물보호제·비료·종자)이고, 금융업·공공기관·
복지법인 고유 규제나 타사 홍보성 보도는 필요 없어.

§1부터 순서대로 하되, §4(내부통제 키워드 정밀화)가 효과가 가장 클 것 같으니
먼저 해보고 결과를 보여줘. 그다음 나머지 진행하자.

필터 단계별로 몇 건이 걸러지는지 로그로 보여줘.
과하게 걸러지는 게 더 위험하니 애매하면 통과시키고 점수를 낮춰줘.
```
