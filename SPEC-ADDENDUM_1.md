# SPEC 보완서 — 공식 정보원 우선 수집으로 전환
## Policy Watch v1.1 (SPEC.md에 이어서 적용)

> **적용 방법**
> 이 파일을 `C:\policy-watch\SPEC-ADDENDUM.md` 로 저장한 뒤 Claude Code에 아래 한 줄을 입력.
> `SPEC-ADDENDUM.md 읽고 Phase 3A부터 진행해줘`
>
> **기존 SPEC.md는 그대로 둔다.** 이 문서는 §3(수집 정책)과 §8(작업 순서)만 덮어쓰고, 나머지(§4 스키마, §5 유틸, §7 프론트엔드, §9 주의사항)는 원본이 계속 유효하다.

---

# 0. 왜 바꾸는가

Phase 2 실데이터 검증 결과, 뉴스 기반 수집만으로는 다음 문제가 확인됐다.

1. 세법·ESG 카테고리가 각각 100건으로 상한에 걸렸고, 그 대부분이 4순위 종합뉴스였다.
2. `세무조사` 키워드가 검찰 수사·압수수색 기사를 끌어왔다. (예: "세무조사 로비 의혹 계열사 수사")
3. 실무자가 실제로 조치해야 할 항목과 단순 보도가 구분되지 않는다.

**전환 방향: 공식 기관 원문이 본진, 뉴스는 해설·보조.**

뉴스 수집을 폐기하지는 않는다. 공식 발표는 원문 PDF 한 덩어리로 나오는 경우가 많아, 전문지의 해설 기사가 실무 이해에 먼저 도움이 되기 때문이다.

---

# 1. 수집 소스 3계층 구조

| 계층 | 성격 | 소스 | 대시보드 취급 |
|---|---|---|---|
| **L1 공식 원문** | 기관이 직접 공표한 기준·법령·지침 | 회계기준원, 금감원, 금융위, 기재부, 국세청, 법제처 | **무조건 노출.** 노이즈 필터 면제, 상한 미적용 |
| **L2 공식 보도자료** | 정부 부처 발표문 | 정책브리핑(korea.kr) 경유 | 무조건 노출, 노이즈 필터 면제 |
| **L3 해설·보도** | 전문지/일반지 기사 | 네이버·구글 뉴스 | **보조.** 카테고리당 상한 적용, 노이즈 필터 적용 |

## L3 축소 규칙 (`_config.py`에 반영)

```python
# 뉴스(L3)는 보조 역할이므로 상한을 크게 낮춘다
MAX_NEWS_PER_CATEGORY = 15      # 기존 60 → 15
MAX_TIER4_PER_CATEGORY = 5      # 종합 경제지는 카테고리당 최대 5건

# L1/L2 공식 자료는 상한 없음 (전량 노출)
MAX_OFFICIAL_PER_CATEGORY = None
```

정렬 시 L1/L2를 항상 L3보다 위에 놓는다. `final_score` 비교 이전에 계층으로 먼저 가른다.

---

# 2. 우선 수집 대상 공식 사이트

각 소스에 대해 **먼저 접근 가능성을 조사(probe)한 뒤 구현**한다. RSS가 있으면 RSS, 없으면 목록 페이지 HTML 파싱, 그것도 막히면 대체 경로를 찾는다. 조사 결과를 `docs/SOURCE_PROBE.md`에 기록할 것.

## A. IFRS / K-IFRS 회계기준 → `kifrs` 카테고리

| # | 소스 | 대상 | 비고 |
|---|---|---|---|
| A1 | 한국회계기준원 (kasb.or.kr) | 보도자료 / 공지사항 / 기준서 제·개정 공표 | **1순위 핵심 소스** |
| A2 | 한국회계기준원 K-IFRS 페이지 | 기준서별 상세내용, 개정 이력 | 기준서 번호(제1116호 등) 추출 |
| A3 | 회계기준 열람서비스 / 질의회신요약 | 질의회신(Q&A) 신규 등록분 | 실무 가치 높음. 갱신 주기 확인할 것 |
| A4 | 금융위원회 (fsc.go.kr) | 회계 관련 보도자료 | L2 정책브리핑으로 대체 가능하면 그쪽 우선 |

**참고:** IFRS 영문 원문(IFRS Foundation)은 저작권 대상이므로 **본문을 수집·저장하지 않는다.** 제목과 링크만 다룬다.

## B. 내부회계관리제도 → `icfr` 카테고리

| # | 소스 | 대상 | 비고 |
|---|---|---|---|
| B1 | 금융감독원 (fss.or.kr) 내부회계관리제도 기준 페이지 | 설계·운영, 평가·보고, 감사인 검토·감사 기준 | **1순위 핵심 소스** |
| B2 | 금감원 자료실 | 평가·보고 지침 개정본, Clean ver. 첨부파일 | 첨부파일명·등록일 함께 수집 |
| B3 | 내부회계관리제도운영위원회 | 모범규준, 적용 사업연도 안내 | 갱신 빈도 낮음. 주 1회 확인으로 충분 |

**적용 사업연도 정보는 `effective_date`로 반드시 추출한다.** (예: "2024년 1월 1일 이후 개시 사업연도부터 적용")

## C. ESG 공시 / 지속가능성 공시 → `esg` 카테고리

| # | 소스 | 대상 | 비고 |
|---|---|---|---|
| C1 | 한국회계기준원 지속가능성 공시기준 업무 페이지 | KSSB 기준 제·개정, 적용지침 | **1순위 핵심 소스** |
| C2 | 금융위원회 ESG 관련 보도자료 | 국내 공시 로드맵, Scope 3 논의 동향 | L2 경유 |
| C3 | KSSB 기준 관련 자료 | 제1호(일반사항), 제2호(기후), 제101호(선택 공시사항) 공개초안 및 확정본 | 호수 번호를 `title`에 유지 |

## D. 세법개정 → `tax` 카테고리

| # | 소스 | 대상 | 비고 |
|---|---|---|---|
| D1 | 기획재정부 (moef.go.kr) 세제개편안 페이지 | 연도별 세제개편안 및 상세본 | **1순위 핵심 소스.** 명칭은 '기획재정부'(재정경제부 아님) |
| D2 | 국세청 (nts.go.kr) 개정세법 해설 | 연도별 개정세법 해설 자료집 | PDF 링크 + 발간연도 |
| D3 | 정책브리핑 (korea.kr) | 세제개편 관련 시행령 개정 추진, 입법·시행령 후속 절차 | L2 통합 수집기로 처리 |
| D4 | 법제처 Open API (law.go.kr) | 법인세법·부가가치세법·조특법 등 제·개정 및 시행일 | 기존 SPEC §3 계획 유지 |

---

# 3. 정책브리핑(korea.kr) 통합 수집기 — 신규

부처별로 각각 긁는 대신, 정책브리핑이 부처 보도자료를 한곳에 모아두는 점을 활용한다.

**장점**
- 기재부·국세청·금융위 보도자료를 단일 구조로 수집 → 파서 하나로 3개 부처 커버
- 부처 홈페이지 개편에 영향을 덜 받음
- 게시 형식이 일정해 날짜·부처명 추출이 안정적

**구현 지침**
```
sources/policy_briefing.py

- 부처 필터: 기획재정부, 국세청, 금융위원회, 금융감독원
- 카테고리 분류: 기존 _utils.classify() 재사용
- source.name은 원 발표 부처명으로 기록 (정책브리핑이 아님)
- source.tier는 원 부처 기준으로 1순위 부여
- urls.official은 원 부처 원문 링크가 있으면 그것을, 없으면 정책브리핑 링크
```

접근 방식(RSS/API/HTML)은 probe 결과에 따라 결정한다.

---

# 4. 공통 구현 규칙

## 4-1. 소스별 어댑터 구조

각 공식 사이트는 다음 인터페이스를 따르는 어댑터로 구현한다. 사이트 구조가 바뀌어도 한 파일만 고치면 되도록 격리한다.

```python
# sources/official/<site>.py

def probe() -> dict:
    """접근 가능성 조사. 실제 수집 전에 먼저 실행.
    returns {"ok": bool, "method": "rss"|"html"|"api", "note": str}
    """

def fetch() -> list[dict]:
    """SPEC.md §4 item 스키마에 맞는 dict 리스트 반환.
    실패 시 예외를 던지지 말고 빈 리스트 + 로그를 남긴다.
    """
```

## 4-2. 공식 자료의 `doc_type` 판정

| 조건 | doc_type |
|---|---|
| 법제처 API 출처 | `법령` |
| 기준서 제·개정 공표 (회계기준원, KSSB) | `기준·가이드라인` |
| 금감원 지침·모범규준 | `기준·가이드라인` |
| 질의회신, 예규, 유권해석 | `예규·유권해석` |
| 부처 보도자료 | `보도자료` |
| 그 외 뉴스 | `기사` |

## 4-3. 첨부파일 처리

공식 자료는 본문이 짧고 PDF/HWP 첨부에 내용이 담기는 경우가 많다. **파일을 내려받아 파싱하지 않는다.** 대신 스키마에 다음을 추가한다.

```json
"attachments": [
  { "name": "내부회계관리제도 평가·보고 지침(Clean ver.).pdf", "url": "https://..." }
]
```
- 없으면 `null`
- 프론트엔드는 카드 하단에 파일명을 작은 링크로 표시 (SPEC §7-3 카드 구성의 버튼 행 위)

## 4-4. 시행일 추출 강화

공식 자료의 가장 큰 가치는 **정확한 시행일**이다. 다음 패턴을 정규식으로 추출해 `effective_date`에 넣는다.

```
"2027년 1월 1일 이후 개시하는 사업연도부터"   → 2027-01-01
"2026. 7. 1. 시행"                            → 2026-07-01
"공포 후 6개월이 경과한 날부터 시행"          → 공포일 + 6개월 (promulgation_date 필요)
"2026년 1월 1일부터 적용"                     → 2026-01-01
```
추출 실패 시 `null`로 두고, `data/schedules_manual.yml`에 수동 보완할 수 있게 로그로 남긴다.

## 4-5. 정부 사이트 접근 제약

SPEC §9-3에 적은 대로, GitHub Actions(미국 IP)에서 한국 정부 사이트가 차단될 수 있다.

- `PROXY_BASE` 시크릿이 있으면 프록시 경유
- 없으면 직접 호출 → 실패 시 `meta.sources_failed`에 기록하고 나머지 진행
- **로컬(한국 IP)에서는 되는데 Actions에서 안 되는 상황**이 정상적으로 발생할 수 있으므로, 로컬 성공만으로 완료 판정하지 말 것

## 4-6. 크롤링 예의

- `User-Agent` 명시, 요청 간 1초 sleep (공식 사이트는 뉴스보다 여유 있게)
- `robots.txt` 확인 및 준수
- 하루 1회 수집이므로 부하 문제는 없으나, 개발 중 반복 테스트 시 응답을 로컬 캐시에 저장해 재요청을 줄일 것

---

# 5. 세법 카테고리 노이즈 보강

Phase 2에서 확인된 문제를 반영한다.

## 5-1. 사건·사고 기사 제외 (L3에만 적용)

```python
# _config.py에 추가
INCIDENT_KEYWORDS = [
    "검찰", "수사", "압수수색", "구속", "기소", "혐의", "고발",
    "로비", "비리", "은닉", "포탈", "탈세 혐의", "구형", "선고",
]
```

**적용 규칙**
- L3(뉴스)에만 적용. L1/L2 공식 자료는 면제
- 제목에 `INCIDENT_KEYWORDS`가 있고, 동시에 `개정`·`시행령`·`예규`·`지침` 등 제도 관련 키워드가 **없으면** 제외
- 단순 배제가 아니라 AND 조건인 이유: "탈세 혐의 판결에 따른 예규 변경" 같은 실무 관련 기사를 잃지 않기 위함

## 5-2. `세무조사` 키워드 조정

`세무조사`는 단독으로 두지 말고 다음 조합으로만 매칭한다.

```
세무조사 + (사전통지 | 대상 선정 | 운영규정 | 절차 | 기간 연장 | 납세자권리)
```

---

# 6. 작업 순서 (SPEC §8 대체)

| Phase | 산출물 | 완료 조건 |
|---|---|---|
| **3A** | `docs/SOURCE_PROBE.md` — 위 §2의 모든 소스에 대한 접근 가능성 조사 | 소스별 RSS/HTML/API 여부, 차단 여부, 갱신 주기를 표로 정리. **코드 작성 전에 이것부터** |
| **3B** | `sources/official/kasb.py` (A1~A3), `sources/official/fss.py` (B1~B3) | 회계기준원·금감원에서 실제 항목 수집, 제목·날짜·링크 콘솔 출력 |
| **3C** | `sources/policy_briefing.py` (§3), `sources/official/moef.py`, `nts.py` (D1~D2) | 4개 부처 보도자료 수집 확인 |
| **3D** | `sources/law_api.py` (D4) + 프록시 연동 | 법제처에서 시행일 포함 항목 수집 |
| **3E** | `_config.py` 개정 — L1/L2/L3 계층, 상한값, `INCIDENT_KEYWORDS` | 기존 테스트 전부 통과 + 신규 규칙 테스트 추가 |
| **4** | `_summarize.py`, `schedules.py`, `main.py` → `site/data.json` | 스키마 검증 통과. **L1/L2가 상단에 오는지 확인** |
| **5** | 프론트엔드 (SPEC §7 그대로) | 더미 데이터로 UI 완성 후 실데이터 연결 |
| **6** | GitHub Actions + Pages | 수동 실행 성공, 자동 커밋 확인 |

**Phase 3A가 가장 중요하다.** 조사 없이 구현하면 사이트 구조를 잘못 짚어 시간을 크게 낭비한다.

---

# 7. Phase 3A 조사 결과 기록 양식

`docs/SOURCE_PROBE.md`를 아래 형식으로 작성할 것.

```markdown
| ID | 사이트 | 대상 페이지 | 접근 방식 | 상태 | 갱신 주기 | 비고 |
|----|--------|------------|----------|------|----------|------|
| A1 | 회계기준원 | 보도자료 | HTML 목록 파싱 | OK | 주 1~2회 | 페이지네이션 있음 |
| A3 | 회계기준원 | 질의회신요약 | 로그인 필요? | 확인필요 | - | 접근 제약 여부 확인 |
| B1 | 금감원 | 내부회계 기준 | HTML | OK | 분기 | 첨부 PDF 위주 |
...
```

각 항목에 대해 반드시 명시할 것:
- 실제 요청을 보내 HTTP 상태 코드를 확인했는가
- 목록 페이지에서 **제목 / 등록일 / 상세 링크** 3요소를 추출할 수 있는가
- 페이지 구조가 자주 바뀔 가능성이 있는가 (JS 렌더링 여부)

---

# 8. Claude Code에 입력할 지시

```
SPEC-ADDENDUM.md 읽어줘.

뉴스 중심에서 공식 기관 중심으로 방향을 바꾼다.
기존 코드는 버리지 말고 그대로 두되, 뉴스는 보조 역할로 내린다.

Phase 3A부터 시작해줘. 코드 작성 전에 §2의 소스들을 실제로 접근해보고
docs/SOURCE_PROBE.md에 조사 결과부터 정리해줘.
```
