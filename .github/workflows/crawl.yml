# STATUS: Phase 0 스캐폴딩 placeholder — Phase 6에서 SPEC.md §1/§8 요구사항으로 구현 예정.
# 최종 형태: cron '0 22 * * *' (07:00 KST) + workflow_dispatch,
#           python -m sources.main 실행 → site/data.json 커밋 & 푸시.
name: crawl

on:
  workflow_dispatch: {}

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Phase 6에서 구현 예정 (SPEC.md §8)"
