# SPEC 보완서 2 — 수집 대상 문서 종류 및 진행 단계 정의
## Policy Watch v1.2 (SPEC-ADDENDUM.md에 이어서 적용)

> **적용 방법**
> `C:\policy-watch\SPEC-ADDENDUM-2.md` 로 저장 후 Claude Code에 입력:
> `SPEC-ADDENDUM-2.md 읽고 Phase 3A 진행해줘`
>
> 이 문서는 SPEC-ADDENDUM.md의 §2(수집 대상 사이트)를 **문서 종류 단위로 구체화**한다.
> 나머지 내용(3계층 구조, 정책브리핑 통합, 노이즈 보강, Phase 순서)은 그대로 유효하다.

---

# 1. 핵심 원칙 두 가지

## 1-1. 사이트가 아니라 문서 종류로 수집한다

기존에는 "회계기준원 보도자료 게시판"처럼 게시판 단위로 접근했다. 그러나 실무자가 필요로 하는 것은 게시판이 아니라 **특정 종류의 문서**다. 같은 게시판에 공개초안과 교육자료가 섞여 있고, 반대로 하나의 문서 종류가 여러 게시판에 흩어져 있다.

따라서 각 어댑터는 **수집 후 문서 종류를 판정**하고, 그 결과를 `doc_type`에 기록한다.

## 1-2. 확정본과 초안을 반드시 구분한다

공개초안·검토의견은 **아직 확정되지 않은 자료**다. 확정 기준서와 나란히 놓으면 실무 판단을 그르칠 수 있다. 동시에 공개초안은 가장 일찍 알아야 할 조기 경보이기도 하다.

그래서 문서 종류와 별개로 **진행 단계(`stage`)** 를 별도 필드로 관리한다. 프론트엔드는 이를 카드 상단에 뱃지로 표시한다.

---

# 2. 스키마 확장 (SPEC.md §4에 추가)

`items[]` 각 항목에 다음 두 필드를 추가한다.

```json
{
  "doc_type": "공개초안",
  "stage": "의견수렴",
  ...
}
```

## 2-1. `doc_type` — 문서 종류 (전 카테고리 공통 열거값)

| 값 | 설명 | 실무 우선순위 |
|---|---|---|
| `제·개정` | 기준서·법령의 확정 제정 또는 개정 | **최상** |
| `공개초안` | Exposure Draft, 개정안, 입법예고 | **최상** (조기 경보) |
| `검토의견` | 공개초안에 대한 의견서, 의견수렴 결과 | 중 |
| `적용지침` | 시행지침, 평가·보고 지침, 적용 가이드 | 상 |
| `모범규준` | 설계·운영 모범규준 등 | 상 |
| `질의회신` | Q&A, 질의회신요약, 유권해석, 예규 | **상** (실무 직결) |
| `FAQ` | 기관이 정리한 자주 묻는 질문 | 상 |
| `예시서식` | 템플릿, 서식, 예시 문서 | 중 |
| `감사·검토기준` | 감사인 검토·감사 관련 기준 | 상 |
| `해설·교육자료` | 해설서, 교육자료, 설명회 자료 | 중 |
| `로드맵·일정` | 도입 일정, 단계별 적용 계획 | **상** |
| `보도자료` | 부처 발표문 | 중 |
| `결정례·판례` | 조세심판원 결정, 주요 판례 | 중 |
| `기사` | 언론 보도 (L3) | 하 |

## 2-2. `stage` — 진행 단계

| 값 | 조건 | 프론트엔드 뱃지 색상 |
|---|---|---|
| `의견수렴` | 공개초안·입법예고 상태, 의견 접수 기간 중 | 주황 (`#c2410c`) |
| `확정` | 제·개정 공표 완료, 시행일 미도래 또는 불명 | 네이비 (`--navy`) |
| `시행예정` | 확정 + `effective_date`가 오늘 이후 | 청록 (`#0e7490`) |
| `시행중` | 확정 + `effective_date`가 오늘 이전 | 회색 (`#64748b`) |
| `참고` | 해설자료·FAQ·교육자료 등 단계 개념이 없는 것 | 연회색, 뱃지 생략 가능 |

**판정 규칙**
```
doc_type이 공개초안·검토의견   → stage = 의견수렴
doc_type이 제·개정             → effective_date 유무로 확정/시행예정/시행중 판정
doc_type이 적용지침·모범규준·감사검토기준 → 확정 계열로 판정
그 외                          → 참고
```

`stage`는 날짜에 따라 바뀌므로 **`data.json`에는 `확정` 여부까지만 저장하고, `시행예정`/`시행중` 구분은 프론트엔드에서 `effective_date`로 계산**한다. (D-Day를 저장하지 않는 것과 같은 이유)

## 2-3. 프론트엔드 반영 (SPEC.md §7-3 카드 구성 수정)

카드 상단 행을 다음 순서로 구성한다.

```
[카테고리 뱃지] [문서종류 뱃지] [단계 뱃지]        출처: 한국회계기준원 [공식]
```

- 문서종류 뱃지: 아웃라인 스타일, 테두리만 얇게
- 단계 뱃지: 위 표의 색상으로 채움. `참고`는 표시하지 않음
- 좌측 필터 영역에 **문서종류 다중선택 드롭다운** 추가 (기본값: 전체)
  - 자주 쓰는 조합을 빠른 버튼으로: `제·개정만` `질의회신만` `공개초안만`

---

# 3. 카테고리별 수집 대상 문서

## ① K-IFRS / 회계기준 → `kifrs`

**수집 대상**

| 문서 종류 | 출처 | 비고 |
|---|---|---|
| K-IFRS 제·개정 자료 | 한국회계기준원 | **필수** |
| 공개초안 | 한국회계기준원 | 기준서 번호 유지 (제1116호 등) |
| 검토의견 | 한국회계기준원 | 공개초안과 연결되면 `related_id`로 묶기 |
| 적용지침 | 한국회계기준원 | |
| 질의회신 | 회계기준 열람서비스 / 질의회신요약 | **실무 가치 최상** |
| 실무 교육·해설자료 | 한국회계기준원 | |

**제외 대상**
- **IFRS Foundation 영문 원문 자료는 수집하지 않는다.** 저작권 대상이며 한국 내 접근 조건이 별도로 있다. 한국회계기준원이 국내 공표한 자료만 다룬다.
- 회계법인 홍보성 자료(세미나 안내, 서비스 소개)는 제외

## ② 내부회계관리제도 → `icfr`

**수집 대상**

| 문서 종류 | 출처 | 비고 |
|---|---|---|
| 설계·운영 모범규준 | 내부회계관리제도운영위원회 | **필수** |
| 평가·보고 지침 | 금융감독원 | 개정본 / Clean ver. 구분해 기록 |
| FAQ | 금감원 / 운영위원회 | |
| 예시 서식 | 금감원 / 운영위원회 | 첨부파일 링크 위주 |
| 감사 및 검토기준 | 금융감독원 | 감사인 대상 기준 |
| 연결 내부회계 관련 자료 | 금감원 / 운영위원회 | **적용 사업연도 반드시 추출** |

**특기사항**
- 이 카테고리는 갱신 빈도가 낮으므로(분기~반기) 매일 수집하되 변경 감지 위주로 처리한다.
- 개정본이 올라오면 **이전 버전과의 차이**가 실무 관심사다. 제목에 `개정`, `ver.`, `(안)` 등이 있으면 `title`에 그대로 유지할 것.
- "○○년 1월 1일 이후 개시하는 사업연도부터 적용" 문구는 반드시 `effective_date`로 파싱한다.

## ③ ESG 공시기준 → `esg`

**수집 대상**

| 문서 종류 | 출처 | 비고 |
|---|---|---|
| KSSB 기준 제·개정 | 한국회계기준원 (KSSB) | **필수** |
| 국내 지속가능성 공시기준 | 한국회계기준원 | 제1호·제2호·제101호 등 호수 유지 |
| 공개초안 | 한국회계기준원 (KSSB) | |
| 적용지침 | 한국회계기준원 | |
| 검토의견 | 한국회계기준원 | |
| 국내 도입 일정 및 로드맵 | 금융위원회 | **`로드맵·일정` doc_type, 우측 캘린더 연동** |

**특기사항**
- 도입 일정·로드맵은 자산규모별 단계 적용이 예정되어 있어, **적용 대상 구간 정보**가 있으면 `description`에 유지한다.
- ISSB 영문 원문은 K-IFRS와 같은 이유로 수집하지 않는다. 국내 기관이 공표한 자료만 다룬다.

## ④ 세법 → `tax`

> **확인 필요:** 원본 문서에서 이 항목이 확인되지 않아 아래는 기본안이다. 실제 요구사항이 다르면 이 표만 수정하면 된다.

| 문서 종류 | 출처 | 비고 |
|---|---|---|
| 세제개편안 | 기획재정부 | **필수.** 연도별 개편안 및 상세본 |
| 시행령 개정안 / 입법예고 | 기획재정부, 정책브리핑 | `공개초안` 처리 |
| 개정세법 해설 | 국세청 | 연도별 자료집 |
| 예규·유권해석 | 국세청 | **실무 가치 최상** |
| 집행기준 | 국세청 | |
| 법령 제·개정 및 시행일 | 법제처 Open API | 법인세법·부가가치세법·조특법 등 |
| 결정례 | 조세심판원 | 주요 결정 위주 |

---

# 4. 문서 종류 판정 규칙

각 어댑터는 수집한 항목의 **제목**을 기준으로 `doc_type`을 판정한다. `_config.py`에 다음을 추가한다.

```python
# 앞쪽 규칙이 우선한다 (순서 중요)
DOC_TYPE_RULES = [
    ("공개초안",      ["공개초안", "공개 초안", "개정안", "제정안", "입법예고", "(안)", "의견조회", "의견수렴"]),
    ("검토의견",      ["검토의견", "의견서", "의견수렴 결과", "코멘트"]),
    ("적용지침",      ["적용지침", "시행지침", "평가·보고 지침", "평가 및 보고", "적용 가이드", "가이드라인"]),
    ("모범규준",      ["모범규준", "설계·운영", "설계 및 운영"]),
    ("감사·검토기준", ["감사기준", "검토기준", "감사인", "검토·감사"]),
    ("질의회신",      ["질의회신", "질의 회신", "유권해석", "예규", "서면질의", "질의응답"]),
    ("FAQ",          ["FAQ", "자주 묻는", "Q&A"]),
    ("예시서식",      ["예시", "서식", "템플릿", "양식", "샘플"]),
    ("로드맵·일정",   ["로드맵", "도입 일정", "적용 일정", "단계별 적용", "시행 일정"]),
    ("해설·교육자료", ["해설", "교육", "설명회", "안내서", "핸드북", "실무"]),
    ("결정례·판례",   ["결정례", "판례", "심판례", "조세심판"]),
    ("제·개정",       ["제정", "개정", "공표", "의결", "확정"]),
    ("보도자료",      ["보도자료", "보도참고", "발표"]),
]

def doc_type_of(title: str, source_tier: int) -> str:
    """제목 기준 판정. 어디에도 안 걸리고 tier>=4면 '기사'."""
    t = _norm(title)
    for dtype, kws in DOC_TYPE_RULES:
        if any(_norm(k) in t for k in kws):
            return dtype
    return "기사" if source_tier >= 4 else "보도자료"
```

**주의:** 순서가 중요하다. "K-IFRS 제1116호 개정안 공개초안"은 `개정`도 포함하지만 `공개초안`이 먼저 걸려야 한다. 위 순서는 이를 반영한 것이다.

## 4-1. 테스트 필수 케이스

`tests/test_doc_type.py`에 다음을 반드시 포함한다.

| 제목 예시 | 기대 `doc_type` | 확인 목적 |
|---|---|---|
| K-IFRS 제1116호 개정안 공개초안 | `공개초안` | 개정보다 공개초안 우선 |
| 내부회계관리제도 평가·보고 지침 개정 | `적용지침` | 개정보다 적용지침 우선 |
| 법인세법 시행령 일부개정령안 입법예고 | `공개초안` | 입법예고 판정 |
| 지속가능성 공시기준 제2호 제정 공표 | `제·개정` | 확정본 판정 |
| 내부회계관리제도 FAQ | `FAQ` | |
| 2026년 개정세법 해설 | `해설·교육자료` | |
| 국내 ESG 공시 도입 로드맵 | `로드맵·일정` | |

---

# 5. IFRS Foundation / ISSB 원문 취급 원칙

명시적으로 규정한다.

- **본문을 수집·저장·재배포하지 않는다.**
- 한국회계기준원이 국내 공표한 번역본·요약본만 다룬다.
- 국제 기준 동향을 참고로 표시해야 할 경우, **제목과 원문 링크만** 다루고 본문은 옮기지 않는다.
- 이는 저작권 준수 사항이며 예외를 두지 않는다.

---

# 6. Phase 3A 조사 항목 보강

SPEC-ADDENDUM.md §7의 조사 양식에 다음 열을 추가한다.

| 추가 열 | 내용 |
|---|---|
| 수집 대상 문서종류 | 그 페이지에서 어떤 `doc_type`이 나오는지 |
| 종류 판정 가능성 | 제목만으로 판정 가능한지, 상세 페이지를 열어야 하는지 |
| 시행일 노출 위치 | 목록에 있는지, 상세 페이지에 있는지, 첨부파일 안에만 있는지 |

**시행일이 첨부 PDF 안에만 있는 경우**는 자동 추출이 어렵다. 이런 소스는 조사 단계에서 표시해두고, `data/schedules_manual.yml`에 수동 관리 대상으로 넘긴다.

---

# 7. Claude Code에 입력할 지시

```
SPEC-ADDENDUM-2.md 읽어줘.

수집 대상을 게시판 단위가 아니라 문서 종류 단위로 바꾼다.
doc_type과 stage 필드가 추가되고, 공개초안과 확정본을 구분해야 한다.

Phase 3A부터 진행해줘. 코드 작성 전에 §3의 문서 종류들이 각 사이트
어디에 있는지 조사해서 docs/SOURCE_PROBE.md에 정리해줘.
```
